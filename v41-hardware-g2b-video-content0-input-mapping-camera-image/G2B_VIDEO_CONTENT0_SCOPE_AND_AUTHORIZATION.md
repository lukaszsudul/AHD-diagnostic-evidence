
# Scope and authorization

Authorized work was limited to offline input/route analysis, one exact
primary-only rolling 2500-record baseline capture, private pixel analysis,
safe optional NVP diagnostics only if an existing documented and reversible
runtime service existed, final cleanup, and sanitized evidence publication.
The runtime NVP service prerequisite did not exist, so no test-pattern or
channel-routing write was executed. Fresh captures executed: 1 of 6 maximum.
