
# Channel scan report

Runtime NVP channel scan supported = NO. Bank1 C2 routing is documented in the
silicon and startup table, but the exact PRODUCT image exposes no governed
host-to-NVP runtime transaction interface. Per-channel status is also
unavailable, so no channel was selected, captured, or restored. NVP channels
tested = NONE. Input mapping mismatch = UNRESOLVED.
