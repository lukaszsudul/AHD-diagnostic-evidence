# Compatibility classification

Classification: `NVP6134_REFERENCE_ONLY_CONTROLLED_COMPATIBILITY_TEST`

The private slice semantics are reference-only and are not directly documented for NVP6134C. That fact alone is not a blocker under this task. The blocker is narrower and explicit: the frozen reference context proves an inseparable functional write outside the Owner-authorized register set.

First blocker: `ACQ1_COMPAT0_SLICE_ACTION_EXCEEDS_AUTHORIZED_WRITE_SET`
