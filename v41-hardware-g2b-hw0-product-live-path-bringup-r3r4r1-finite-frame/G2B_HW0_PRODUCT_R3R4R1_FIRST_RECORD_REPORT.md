# R3R4R1 persistent first-record hardware report

Result: `NOT_REACHED`.

The mandatory offline suite stopped at `PARENT_QUIESCENCE_HANDSHAKE_PASS` with
`R3R4R1_CAPTURE_TOOL_HARD_GATE_FAILED`. The governed order prohibited architecture-gate advancement and
the first DUT connection. No current DUT value is inferred from predecessor
evidence.
The offline synthetic first-record case passed, including fsync and ABI validation. It is not a hardware result. No raw bytes are published.
