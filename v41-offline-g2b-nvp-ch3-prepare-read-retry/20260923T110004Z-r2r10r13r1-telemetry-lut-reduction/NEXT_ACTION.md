# Next action

The R13R1 candidate stopped at 20,845 Slice LUT after the one authorized `opt_design`; the strict gate is `< 20,800`. No bitstream exists.

One future, separately authorized source revision would need at least 46 additional post-opt LUT of reduction while preserving the full R13 retry contract and ABI v1, followed by a new build. Do not program hardware from this package.
