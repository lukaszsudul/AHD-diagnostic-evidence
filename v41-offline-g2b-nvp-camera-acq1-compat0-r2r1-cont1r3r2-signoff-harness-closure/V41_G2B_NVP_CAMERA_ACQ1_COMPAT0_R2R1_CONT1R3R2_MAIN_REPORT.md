# AHD v41 CONT1R3R2 — offline sign-off comparison closure

## Decision

`HARNESS_MIXED_VIEW_DEFECT_CONFIRMED`. The old sign-off failure arose from comparing different timing-database views, not from a demonstrated changed effective constraint on the final provisional DCP. The exact unchanged 17,469,233-byte checkpoint at SHA-256 `C09145B1397E2A8DC972E435E755917C9104A5FDA2E5EF297FE4628EF9672CC2` meets the scoped existing-DCP sign-off gates and is eligible **only for a separately authorized bitstream-generation task** once this package is pushed and verified by commit-pinned remote byte read-back. No bitstream was generated and no DUT was contacted.

## Authority and method

Frozen source commit `09cd7cbb426027acaefd0cf3989579b80a451f3a`, tree `c6be008ddc387c1f43eefa35d4fed0e2ccb968db`, and exact Vivado 2025.2 build 6299465 were verified. Original DCPs and byte-identical private task-local copies were checked by size and SHA-256 before the read-only work. The final checkpoint is the candidate; the earlier routed checkpoint is reference evidence only. The historical FAIL and `DO_NOT_PROGRAM_UNQUALIFIED_DCP.md` were not altered.

The full [hash flow](HASH_FLOW.csv) ties each historical producer and view to its input DCP and output. The first wrapper compared as-open B to post-bus-skew-restoration A. The third finalizer compared restored A to the final checkpoint's as-open B after `write_checkpoint`/reopen. The earlier helper had both broad text canonicalization and `reset_timing`/`read_xdc` database mutation. The [A/B finding](A_B_DIFFERENCE_FINDINGS.md) identifies six generated selector aliases present in A but inlined in B. Targeted, order-preserving expansion yields identical 97 executable commands. The [frozen contract](COMPARISON_CONTRACT.md) and [addendum](COMPARISON_CONTRACT_ADDENDUM.md) define the only accepted normalization; no arbitrary hash or broad warning stripping is allowed.

Fresh independent Vivado sessions opened the two DCP copies as stored. No XDC was loaded into either acceptance session, and no implementation or checkpoint write was run. Candidate/reference as-open XDC views both equal B; clock, cell/net/LOC/BEL, route status and complete 119,180-net route-property streams match. Raw full physical XDC files are not claimed byte-identical: their provenance comments and timing-block layout differ. The substantive 49 physical `set_property` and eight waiver commands match with order and scope, and as-open timing commands match. Details and private raw artifact hashes are in [physical findings](PHYSICAL_IDENTITY_FINDINGS.md).

The [paired report comparison](REPORT_COMPARISON.csv) distinguishes raw bytes from ordered bodies. Exceptions, ignored exceptions, clocks, clock networks and CDC bodies match after excluding only generated `Date` and `Command` header fields. Initial timing summaries differed in a cached methodology section; the difference was retained and investigated. After the same read-only methodology report was generated in both fresh as-open sessions, detailed timing-summary bodies matched with warnings intact. The methodology report contained 18 ordinary warnings in each session; its only observed text delta was a floorplan identifier derived from the input checkpoint filename. Bus-skew and promoted checks are explicitly inherited rather than disguised as freshly rerun.

Focused corrected-comparator tests pass 10/10 XDC cases, 4/4 report-body cases and 3/3 authority rejection cases, with real source/DCP/tool authority PASS. An initial order-sensitive test fixture was corrected to require fail-closed behavior; candidate bytes and constraints were not edited. The corrected task-local utility and collector source are part of this package. The existing final candidate reopened independently with the frozen collector and retained the same identity before and after reporting.

## Gate linkage

Timing: fully routed 38,667/38,667, WNS +0.047 ns, TNS 0, WHS +0.036 ns, THS 0, zero internal unconstrained endpoints. Resource gate: LUT 19,738/20,800, below the governed 20,384 ceiling; FF 21,538/41,600; BRAM 28/50; DSP 0/90. DRC: zero errors, zero critical warnings, 24 ordinary warnings. Methodology: zero errors, zero critical warnings, 18 ordinary warnings. CDC: accepted profile-specific semantic disposition, 1,337 raw rows (427 critical, 874 warning, 36 info) with zero unreconciled critical/warning destinations; raw severities remain visible. Active bus-skew: 11/11 met and zero violations in the inherited source-DCP receipt; promoted replacement checks: 17/17 PASS. The latter two were not rerun; their inheritance is tied to identical as-open constraints, complete route-property stream, netlist/placement and source identity. The [closure receipt](DCP_CLOSURE_RECEIPT.md) records the scoped requalification of the *same bytes*.

## Boundary and follow-up

This is checkpoint sign-off, not PRODUCT promotion, not an I2C reliability result, not completion of 1,000 or 10,000 scans, and not a camera frame. New synthesis/opt/place/route: 0/0/0/0. FPGA source/XDC/master/manifest/projection changes: none. Bitstream: none. DUT/SSH/JTAG/programming/reboot/camera actions: none. Current DUT state remains `OWNER_ATTESTED_UNCHANGED_EXCEPT_IP_NOT_SURVEYED_IN_THIS_TASK`; `10.132.1.111:22` is only a future endpoint authority, not contacted here.

The first executable follow-up is a **separately Owner-authorized bounded bitstream-generation/sign-off task using only this unchanged DCP**, with an exact image hash and receipt. The one-page [first-frame handoff](NEXT_FIRST_FRAME_HANDOFF.md) separates that from later governed hardware activation and the goal of a complete genuine camera frame transferred to Linux and visibly decoded. This package does not authorize MODE1, slice, EQ, DMA, capture or DUT access.

Publication is append-only in `lukaszsudul/AHD-diagnostic-evidence/main`; the publication commit SHA and commit-pinned remote byte comparison are recorded in the final task response after they exist, not predeclared inside this commit. SSOT revision 9 at start is rechecked at completion under the current staleness rule; no SSOT/META/PRODUCT file is edited.
