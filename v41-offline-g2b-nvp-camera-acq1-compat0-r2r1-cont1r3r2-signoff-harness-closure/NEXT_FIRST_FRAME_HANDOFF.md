# Next handoff toward one genuine camera frame

CONT1R3R2 is an offline comparison/sign-off task only. If its closure receipt
is `PASS_SIGNOFF_RECOVERED_EXISTING_DCP`, the exact final provisional DCP
bytes at SHA-256
`C09145B1397E2A8DC972E435E755917C9104A5FDA2E5EF297FE4628EF9672CC2`
are requalified for a **subsequent, separately authorized bitstream-generation
task**. No bitstream is generated here; no new image is deployed. If that
receipt is not PASS, resolve its named first blocker before generation.

The first executable follow-up after a PASS is to obtain Owner authorization
for a bounded bitstream-generation/sign-off task using only this unchanged
DCP and the frozen source/tool identity, with bitstream hash and gate receipt.
That task is not automatically a camera campaign. A separately governed
hardware task must begin with a bounded read-only survey at
`10.132.1.111:22`: established host-key authority, hostname, machine-id,
boot ID, locks, project processes, loaded driver and XDMA nodes, then safe
runtime identity. `192.168.1.57` is superseded, not a fallback. Any
contradiction stops intervention. The Owner's continuity attestation is not
a substitute for that survey, and this task has not performed it.

The goal remains one camera on one channel, one complete genuine frame
transferred to Linux and visibly decoded/displayed. A register response,
test pattern, NOVID transition or status screenshot is not a camera frame.
The separately preregistered 1,000-scan campaign characterizes the NACK
mechanism; it does not replace the separate 10,000-scan zero-error
qualification required wherever current governance applies that rule to
functional writes. Neither scan campaign is itself a first-frame
demonstration. A smaller first-image path would need an explicit Owner
decision and cannot silently waive a binding 10,000-scan rule; the rule is
not a physical law proving that an image cannot be obtained earlier.

No MODE1, slice, EQ, capture, DMA, driver or DUT action is pre-authorized by
this handoff. PRODUCT and SSOT are unchanged.
