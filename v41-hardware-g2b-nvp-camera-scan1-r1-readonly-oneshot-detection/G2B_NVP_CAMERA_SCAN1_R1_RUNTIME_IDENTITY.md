# Runtime identity

Runtime identity: `NOT_REACHED`.

The scanner MAGIC, VERSION, CAPABILITIES, read-manifest identity and inherited PRODUCT transport identity were not read from hardware. Offline netlist and bitstream-manifest identities passed; they are not reported as runtime proof.
