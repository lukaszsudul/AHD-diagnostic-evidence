# Authority and scope — CONT1R3R4R4-I2C-OFFLINE

This task is offline only. The user authorized source-frozen real-master simulation, a bounded reset-canary/raw-cause audit, NVP6134C/reference-source and PCB documentary reviews, one future-image design note, and append-only evidence publication. No DUT, SSH, ping, JTAG, real MMIO, device node, driver, programming, reboot, camera capture or physical probe was used. No RTL, firmware, master timeout/filter/retry, scanner manifest, XDC, SSOT, META or PRODUCT file was changed. No synthesis or implementation stage was run.

The accepted FPGA source is `lukaszsudul/FPGA_AHD` commit `09cd7cbb426027acaefd0cf3989579b80a451f3a`, tree `c6be008ddc387c1f43eefa35d4fed0e2ccb968db`; the tracked source worktree was clean at admission. The active checkout is `main` at `be94f88ee8d179f12928ab791bdae27c22cd1762`, not the selected source authority. The compiled frozen-cone master, SCAN1 manifest package and SCAN1 scanner (three distinct files) were verified against their pinned commit Git blob IDs again at closure, so an active-checkout branch label is not substituted for input identity. Their SHA-256 values are in the private artifact manifest. Testbenches, slave, digital line-delay model, pin monitor, case generator and checkers are task-owned, not source edits. XSim 2025.2 SW build 6299465 was used.

The previous offline evidence commit is `74dd150c3438bbea24641cdd8e3bb45e62a5bc65`; the historical hardware evidence commit is `b1b12524d41435aad49d7ce7662af26981682143`. Prior 25 complete scans and 11 recovered REGADDR_NACK events are inherited, not new observations. The prior hardware campaign remains `NOT_ASSESSED_CAMPAIGN_INCOMPLETE`; a physical root cause is not asserted.

At admission `PROJECT_STATE_REV=9` under the governed `README → GOVERNANCE → UPDATE_POLICY → PROJECT_STATE → TRACK_STATUS → CURRENT_INTERFACES` review order. End-state revision and exact source/document hashes are checked again before publication. The original contract, timing rules and 214-case matrix were hashed before the sweep. The three permitted task-owned checker amendments are retained in `HARNESS_AMENDMENTS.md`; no fourth checker/model revision was made. The later CSV export-only join is separately recorded, with original output preserved and all 214 rows linked to per-case logs and hashes.

The study does not qualify NVP-specific clock stretching, analog bus rise time, actual FPGA input thresholds, reset behavior of the assembled DUT, camera acquisition, or a new bitstream. Generic Standard-mode I²C timing is cited separately from the NVP6134C Rev1.0 data sheet; the latter documents no maximum stretch in reviewed material and labels SCL as an input. A configured 20 µs local SCL timeout is not thereby a vendor-approved timeout.
