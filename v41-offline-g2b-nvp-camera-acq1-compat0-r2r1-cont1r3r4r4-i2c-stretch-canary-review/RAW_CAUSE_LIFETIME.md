# Active-profile raw-cause lifetime (offline source trace)

Disposition: `AVAILABLE_BUT_OVERWRITTEN_OR_STALE` at the fixed-master output; `NOT_EXPOSED_IN_ACTIVE_PROFILE` as a sticky failed-command MMIO record. The 4-bit `error_cause` is not a first-failure latch. Frozen master `rtl/v41/nvp_i2c_fixed_master.sv` lines 240–253 clears it on each newly accepted command; ACK and timeout branches set it for only the current command. SCAN1 subsequently issues `RESTORE_ENTRY_BANK` and `VERIFY_ENTRY_BANK` before final host-visible error, so a later live master cause may be zero from restore/verify and cannot be assigned to the failed select write.

Active top `rtl/top/ahd_capture_top_xdma.sv` lines 395–407 connects the master's `diag_i2c_error_cause` to the ACQ/SCAN1 wrapper at lines 1258–1320. SCAN1 `g2b_nvp_camera_scan1.sv` lines 185–198 maps the raw cause to its frozen error nibble; the select failure records bank/register/mapped nibble in `first_error_detail` at lines 370–380 and 726–735. MMIO `0x1202C` exposes that mapped detail (lines 285–291), not the raw cause. `first_error_index/detail` reset at scan start; they are first *mapped* error within the attempt, not a command-bound raw latch. On the incomplete hard stop the successful snapshot generation is not advanced (`PUBLISH_SNAPSHOT` is bypassed); the prior complete snapshot entries can remain stale. Entry telemetry is staged for `READ_GROUP_ENTRIES` at lines 388–403 and 620–660; a group bank-select transaction has no equivalent per-entry raw telemetry record. Autoinit diagnostics in the top are from `v40a_nvp_autoinit` and its separate bus sequence; their NACK counters do not describe a later SCAN1 command.

MMIO reads of `0x1202C` and telemetry are read-side-effect-free according to the shown read mux, but that property does not make their data raw or fresh. `ACK_CLEAR` affects scanner state/flags, not a retained first raw cause. Polling quickly would race restoration and not supply durable ownership/sequence semantics. The historical physical receipt had final mapped detail `0x0005FF0A`, 51 accepted commands, 37 valid entries and unchanged generation 26; it did not preserve failed-command raw cause or pin waveform. Prior R4R3 offline replay shows one compatible raw4 route, not direct physical observation.

Source authority: frozen commit `09cd7cbb426027acaefd0cf3989579b80a451f3a`; unchanged compiled master SHA-256 `8C916DD7E3967AB22FF0ED90D9BC4533FA50ADE3FB0F4EB620D26B35E93363BF`. No DUT MMIO was read in this task.
