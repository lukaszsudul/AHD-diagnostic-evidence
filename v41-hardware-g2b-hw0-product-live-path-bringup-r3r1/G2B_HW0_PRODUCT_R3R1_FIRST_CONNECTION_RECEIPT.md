# R3R1 first-connection receipt

Authenticated DUT connection count: `0`.

The fresh helper passed its static hard gate but was not executed because the
frozen ABI/write-authority conflict was established first.

`FIRST_CONNECTION_USED_R3R1_LOCAL_HELPER = NOT_APPLICABLE_NO_CONNECTION`

`FIRST_CONNECTION_HELPER_BOUNDARY_VIOLATION = NO`

`HARDWARE_ACCESSED = NO`
