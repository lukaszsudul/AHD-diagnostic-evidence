# R3R1 counter reconciliation

Result: `NOT_REACHED`.

No coherent MMIO baseline, first record, finite capture, drain, or post-capture
snapshot was taken. Consequently no counter delta or sequence reconciliation
can be computed. All capture error and fatal fields are `N/A`, not zero.

`COUNTER_RECONCILIATION = NOT_REACHED`
