--- R3R4R1/capture_r3r4.py
+++ R3R4R2/capture_r3r4.py
@@ -1,5 +1,5 @@
 #!/usr/bin/env python3
-"""R3R4R1 persistent 2500-record capture with parent-only MMIO authority."""
+"""R3R4R2 persistent 2500-record capture with parent-only MMIO authority."""
 from __future__ import annotations
 
 import csv
@@ -23,9 +23,9 @@
 
 
 P = Path
-ROOT = P('/home/vcdeagent1/vcde_artifacts/g2b_hw0_product_r3r4r1/20260907T050126Z')
-LOCK = P('/tmp/ahd-g2b-hw0-product-r3r4r1-20260907T050126Z.lock/receipt.json')
-TASK = 'G2B-HW0-PRODUCT-R3R4R1'
+ROOT = P('/home/vcdeagent1/vcde_artifacts/g2b_hw0_product_r3r4r2/20260907T071912Z')
+LOCK = P('/tmp/ahd-g2b-hw0-product-r3r4r2-20260907T071912Z.lock/receipt.json')
+TASK = 'G2B-HW0-PRODUCT-R3R4R2'
 EXPECTED_BOOT = '614295f4-c62b-4430-ae67-06013bea7084'
 ABI_SHA256 = 'AACB8F32CE3807C0A1DACD644FFFA90D214AA599F0798A700576987924E0D2B6'
 RECORD_BYTES = 4096
@@ -104,23 +104,23 @@
 
 def compact_emit(channel, event_type: str, **fields) -> None:
     require(event_type in CONTROL_EVENT_TYPES,
-            'R3R4R1_CONTROL_IPC_EVENT_NOT_ALLOWED')
+            'R3R4R2_CONTROL_IPC_EVENT_NOT_ALLOWED')
     message = {'type': event_type, 'monotonic_ns': time.monotonic_ns(), **fields}
-    require(not _contains_bytes(message), 'R3R4R1_RAW_RECORD_CONTROL_IPC_FORBIDDEN')
+    require(not _contains_bytes(message), 'R3R4R2_RAW_RECORD_CONTROL_IPC_FORBIDDEN')
     encoded = json.dumps(message, sort_keys=True)
     require(len(encoded) < RECORD_BYTES,
-            'R3R4R1_CONTROL_IPC_MESSAGE_NOT_COMPACT')
+            'R3R4R2_CONTROL_IPC_MESSAGE_NOT_COMPACT')
     channel.put(message, timeout=0.5)
 
 
 def read_lock() -> dict:
-    require(LOCK.is_file(), 'R3R4R1_LINUX_LOCK_REQUIRED')
+    require(LOCK.is_file(), 'R3R4R2_LINUX_LOCK_REQUIRED')
     lock = json.loads(LOCK.read_text(encoding='utf-8'))
     require(lock.get('task') == TASK and lock.get('state') == 'HELD',
-            'R3R4R1_LINUX_LOCK_OWNER_MISMATCH')
+            'R3R4R2_LINUX_LOCK_OWNER_MISMATCH')
     boot = P('/proc/sys/kernel/random/boot_id').read_text().strip()
     require(boot == EXPECTED_BOOT and boot == lock.get('boot_id'),
-            'R3R4R1_BOOT_CONTINUITY_LOST')
+            'R3R4R2_BOOT_CONTINUITY_LOST')
     return lock
 
 
@@ -142,7 +142,7 @@
 class MMIO:
     """Parent-owned, allowlisted, fully logged MMIO descriptor."""
 
-    def __init__(self, session: str = 'T34') -> None:
+    def __init__(self, session: str = 'T3T4') -> None:
         self.session = session
         read_lock()
         self.proof = json.loads((ROOT / 'logs/t1-proof.json').read_text())
@@ -253,7 +253,7 @@
             require(precondition == 'POST_RESET_QUIESCENT',
                     'FATAL_W1C_PRECONDITION')
         else:
-            raise GateError('R3R4R1_MMIO_WRITE_ALLOWLIST_VIOLATION')
+            raise GateError('R3R4R2_MMIO_WRITE_ALLOWLIST_VIOLATION')
 
         stamp = time.time_ns()
         base = {
@@ -344,19 +344,19 @@
         self.first_valid = False
         self.first_error = None
         self.last_completed_operation = 'RECORD_SINK_INITIALIZING'
-        self.primary_path = private_dir / 'T34-primary-records.bin'
-        self.drain_path = private_dir / 'T34-drain-records.bin'
-        self.trailing_path = private_dir / 'T34-incomplete-trailing.bin'
-        self.first_path = private_dir / 'T34-first-record.bin'
-        self.payload_path = private_dir / 'T34-first-payload.bin'
-        self.journal_path = private_dir / 'T34-capture-journal.jsonl'
-        self.result_path = private_dir / 'T34-reader-result.json'
+        self.primary_path = private_dir / 'T3T4-primary-records.bin'
+        self.drain_path = private_dir / 'T3T4-drain-records.bin'
+        self.trailing_path = private_dir / 'T3T4-incomplete-trailing.bin'
+        self.first_path = private_dir / 'T3T4-first-record.bin'
+        self.payload_path = private_dir / 'T3T4-first-payload.bin'
+        self.journal_path = private_dir / 'T3T4-capture-journal.jsonl'
+        self.result_path = private_dir / 'T3T4-reader-result.json'
         self.primary_file = self.primary_path.open('xb', buffering=0)
         self.drain_file = self.drain_path.open('xb', buffering=0)
         self.journal = self.journal_path.open('x', encoding='utf-8', newline='\n')
         self.first_queue: queue.Queue = queue.Queue(maxsize=1)
         self.first_thread = threading.Thread(
-            target=self._persist_first, name='r3r4r1-first-record-persister',
+            target=self._persist_first, name='r3r4r2-first-record-persister',
             daemon=False,
         )
         self.first_thread.start()
@@ -365,7 +365,7 @@
 
     def _journal(self, event: str, **fields) -> None:
         row = {'event': event, 'utc_ns': time.time_ns(), **fields}
-        require(not _contains_bytes(row), 'R3R4R1_PRIVATE_JOURNAL_RAW_BYTES_FORBIDDEN')
+        require(not _contains_bytes(row), 'R3R4R2_PRIVATE_JOURNAL_RAW_BYTES_FORBIDDEN')
         self.journal.write(json.dumps(row, sort_keys=True) + '\n')
         self.journal.flush()
 
@@ -379,14 +379,14 @@
             with self.first_path.open('xb', buffering=0) as handle:
                 written = handle.write(blob)
                 require(written == RECORD_BYTES,
-                        'R3R4R1_FIRST_RECORD_SHORT_PERSIST')
+                        'R3R4R2_FIRST_RECORD_SHORT_PERSIST')
                 handle.flush()
                 os.fsync(handle.fileno())
             payload = blob[self.contract.payload_first:self.contract.padding_first]
             with self.payload_path.open('xb', buffering=0) as handle:
                 written = handle.write(payload)
                 require(written == self.contract.payload_bytes,
-                        'R3R4R1_FIRST_PAYLOAD_SHORT_PERSIST')
+                        'R3R4R2_FIRST_PAYLOAD_SHORT_PERSIST')
                 handle.flush()
                 os.fsync(handle.fileno())
             self.first_record_hash = hashlib.sha256(blob).hexdigest().upper()
@@ -400,12 +400,12 @@
             assessment = validator.accept(blob)
             errors = list(assessment.errors) + list(assessment.discontinuity_reasons)
             require(assessment.structurally_valid and not assessment.session_fatal and
-                    not errors, 'R3R4R1_FIRST_RECORD_ABI_VALIDATION_FAILED:' +
+                    not errors, 'R3R4R2_FIRST_RECORD_ABI_VALIDATION_FAILED:' +
                     ';'.join(errors[:8]))
             record = assessment.record
-            require(record is not None, 'R3R4R1_FIRST_RECORD_PARSE_MISSING')
+            require(record is not None, 'R3R4R2_FIRST_RECORD_PARSE_MISSING')
             require(record.reset_epoch == self.armed_epoch,
-                    'R3R4R1_FIRST_RECORD_STALE_EPOCH')
+                    'R3R4R2_FIRST_RECORD_STALE_EPOCH')
             self.first_header = dict(record.header)
             self.first_valid = True
             self.emit('FIRST_RECORD_VALID', valid=True,
@@ -427,8 +427,8 @@
                 pass
 
     def accept(self, chunk: bytes) -> None:
-        require(not self.closed, 'R3R4R1_RECORD_SINK_ALREADY_CLOSED')
-        require(isinstance(chunk, bytes), 'R3R4R1_READER_CHUNK_NOT_BYTES')
+        require(not self.closed, 'R3R4R2_RECORD_SINK_ALREADY_CLOSED')
+        require(isinstance(chunk, bytes), 'R3R4R2_READER_CHUNK_NOT_BYTES')
         if not chunk:
             return
         self.pending.extend(chunk)
@@ -438,7 +438,7 @@
             del self.pending[:RECORD_BYTES]
             if self.primary_count < self.primary_target:
                 written = self.primary_file.write(blob)
-                require(written == RECORD_BYTES, 'R3R4R1_PRIMARY_SHORT_WRITE')
+                require(written == RECORD_BYTES, 'R3R4R2_PRIMARY_SHORT_WRITE')
                 self.primary_hash.update(blob)
                 self.primary_count += 1
                 self._journal('PRIMARY_RECORD_WRITTEN', count=self.primary_count)
@@ -452,9 +452,9 @@
                               primary_bytes=self.primary_count * RECORD_BYTES)
             else:
                 require(self.drain_count < self.drain_limit,
-                        'R3R4R1_DRAIN_RECORD_LIMIT_EXCEEDED')
+                        'R3R4R2_DRAIN_RECORD_LIMIT_EXCEEDED')
                 written = self.drain_file.write(blob)
-                require(written == RECORD_BYTES, 'R3R4R1_DRAIN_SHORT_WRITE')
+                require(written == RECORD_BYTES, 'R3R4R2_DRAIN_SHORT_WRITE')
                 self.drain_hash.update(blob)
                 self.drain_count += 1
                 self._journal('DRAIN_RECORD_WRITTEN', count=self.drain_count)
@@ -475,7 +475,7 @@
         self.first_thread.join(timeout=5.0)
         if self.first_thread.is_alive():
             result = 'BLOCKED'
-            blocker = 'R3R4R1_FIRST_RECORD_PERSISTER_EXIT_TIMEOUT'
+            blocker = 'R3R4R2_FIRST_RECORD_PERSISTER_EXIT_TIMEOUT'
         if self.first_error and result == 'PASS':
             result = 'FAIL'
             blocker = self.first_error['blocker']
@@ -483,7 +483,7 @@
             exception_repr = self.first_error['exception_repr']
             traceback_text = self.first_error['traceback']
         blocker = blocker.strip() if blocker else ''
-        require(bool(blocker), 'R3R4R1_BLANK_BLOCKER_FORBIDDEN')
+        require(bool(blocker), 'R3R4R2_BLANK_BLOCKER_FORBIDDEN')
         try:
             for handle in (self.primary_file, self.drain_file):
                 handle.flush()
@@ -505,7 +505,7 @@
         except BaseException as exc:
             if result == 'PASS':
                 result = 'BLOCKED'
-                blocker = 'R3R4R1_CAPTURE_FINAL_SYNC_FAILED:' + _nonempty_problem(exc)
+                blocker = 'R3R4R2_CAPTURE_FINAL_SYNC_FAILED:' + _nonempty_problem(exc)
                 exception_type = type(exc).__name__
                 exception_repr = repr(exc)
                 traceback_text = traceback_module.format_exc()
@@ -557,14 +557,14 @@
     fd = -1
     sink = None
     result = None
-    blocker = 'R3R4R1_READER_UNFINISHED'
+    blocker = 'R3R4R2_READER_UNFINISHED'
     exception_type = None
     exception_repr = None
     trace = None
     quiet_since = None
     try:
         st = os.stat(c2h_node)
-        require(stat.S_ISCHR(st.st_mode), 'R3R4R1_C2H_NODE_NOT_CHAR_DEVICE')
+        require(stat.S_ISCHR(st.st_mode), 'R3R4R2_C2H_NODE_NOT_CHAR_DEVICE')
         fd = os.open(c2h_node, os.O_RDONLY | os.O_NOFOLLOW)
         emit = lambda event_type, **fields: compact_emit(
             messages, event_type, **fields)
@@ -664,7 +664,7 @@
                 break
             if len(blob) != RECORD_BYTES:
                 raise FailureError(
-                    f'R3R4R1_CAPTURE_FILE_TRAILING_BYTES:{path.name}:{len(blob)}')
+                    f'R3R4R2_CAPTURE_FILE_TRAILING_BYTES:{path.name}:{len(blob)}')
             yield blob
 
 
@@ -672,11 +672,11 @@
                      primary_path: Path, drain_path: Path,
                      trailing_path: Path) -> dict:
     require(primary_path.stat().st_size == PRIMARY_BYTES,
-            'R3R4R1_PRIMARY_FILE_SIZE_INVALID')
+            'R3R4R2_PRIMARY_FILE_SIZE_INVALID')
     require(drain_path.stat().st_size % RECORD_BYTES == 0,
-            'R3R4R1_DRAIN_FILE_SIZE_INVALID')
+            'R3R4R2_DRAIN_FILE_SIZE_INVALID')
     require(trailing_path.stat().st_size == 0,
-            'R3R4R1_INCOMPLETE_TRAILING_BYTES_NONZERO')
+            'R3R4R2_INCOMPLETE_TRAILING_BYTES_NONZERO')
     validator = StreamValidator(contract, armed_epoch=armed_epoch)
     total = 0
     malformed = 0
@@ -735,11 +735,11 @@
                 errors.append('session_fatal')
     drain_records = drain_path.stat().st_size // RECORD_BYTES
     require(total == PRIMARY_TARGET + drain_records,
-            'R3R4R1_HOST_RECORD_COUNT_FILE_MISMATCH')
+            'R3R4R2_HOST_RECORD_COUNT_FILE_MISMATCH')
     if (malformed or padding_errors or gaps or unexpected_epoch or duplicates or
             source_snapshot_changes or errors):
         raise FailureError(
-            'R3R4R1_FULL_RECORD_VALIDATION_FAILED:' +
+            'R3R4R2_FULL_RECORD_VALIDATION_FAILED:' +
             json.dumps({
                 'malformed': malformed, 'padding_errors': padding_errors,
                 'gaps': gaps, 'unexpected_epoch': unexpected_epoch,
@@ -749,7 +749,7 @@
             }, sort_keys=True)
         )
     require(first_header is not None and last_header is not None,
-            'R3R4R1_NO_VALID_RECORDS')
+            'R3R4R2_NO_VALID_RECORDS')
     return {
         'result': 'PASS', 'total_complete_records': total,
         'primary_records': PRIMARY_TARGET, 'drain_records': drain_records,
@@ -804,7 +804,7 @@
         values['last_channel_observed'] == last_channel,
     )
     if not all(checks):
-        raise FailureError('R3R4R1_COUNTER_RECONCILIATION_FAILED:' +
+        raise FailureError('R3R4R2_COUNTER_RECONCILIATION_FAILED:' +
                            json.dumps(values, sort_keys=True))
     return {'result': 'PASS', **values}
 
@@ -819,9 +819,9 @@
         first = False
         event_type = message.get('type')
         require(event_type in CONTROL_EVENT_TYPES,
-                'R3R4R1_CONTROL_IPC_EVENT_NOT_ALLOWED')
+                'R3R4R2_CONTROL_IPC_EVENT_NOT_ALLOWED')
         require(not _contains_bytes(message),
-                'R3R4R1_RAW_RECORD_CONTROL_IPC_FORBIDDEN')
+                'R3R4R2_RAW_RECORD_CONTROL_IPC_FORBIDDEN')
         state.setdefault('events', []).append(message)
         state[event_type] = message
 
@@ -853,14 +853,14 @@
     t2 = json.loads((ROOT / 'logs/t2-result.json').read_text())
     require(proof.get('result') == 'PASS', 'T1_NOT_PASS')
     require(t2.get('result') == 'PASS', 'T2_NOT_PASS')
-    _write_json_exclusive(ROOT / 'logs/T34-start-once.json', {
-        'task': TASK, 'session': 'T34', 'started_utc_ns': time.time_ns(),
+    _write_json_exclusive(ROOT / 'logs/T3T4-start-once.json', {
+        'task': TASK, 'session': 'T3T4', 'started_utc_ns': time.time_ns(),
         'primary_target': PRIMARY_TARGET, 'no_retry': True,
     })
 
     result = {
-        'task': TASK, 'session': 'T34', 'result': 'BLOCKED',
-        'blocker': 'R3R4R1_CAPTURE_UNFINISHED', 'primary_target': PRIMARY_TARGET,
+        'task': TASK, 'session': 'T3T4', 'result': 'BLOCKED',
+        'blocker': 'R3R4R2_CAPTURE_UNFINISHED', 'primary_target': PRIMARY_TARGET,
         'raw_payload_control_ipc': False, 'parent_owned_mmio': True,
         'parent_owned_quiescence': True,
         'last_completed_operation': 'CAPTURE_START_MARKER_CREATED',
@@ -876,12 +876,12 @@
     reader_result = None
     try:
         require(not _open_holders(proof['c2h']),
-                'R3R4R1_C2H_READER_ALREADY_ATTACHED')
-        mmio = MMIO('T34_PARENT')
+                'R3R4R2_C2H_READER_ALREADY_ATTACHED')
+        mmio = MMIO('T3T4_PARENT')
         control = mmio.read(0x380C)
         status = mmio.read(0x3810)
         require(control == 0 and not (status & 0x100),
-                'R3R4R1_DISABLED_BASELINE_NOT_CLEAN')
+                'R3R4R2_DISABLED_BASELINE_NOT_CLEAN')
         pre_reset = {f'0x{offset:04X}': mmio.read(offset)
                      for offset in range(0x3800, 0x385C, 4)}
         result['pre_reset'] = pre_reset
@@ -892,11 +892,11 @@
         deadline = time.monotonic() + 5.0
         while not quiescent(mmio.read(0x380C), mmio.read(0x3810)):
             require(time.monotonic() < deadline,
-                    'R3R4R1_SESSION_RESET_TIMEOUT')
+                    'R3R4R2_SESSION_RESET_TIMEOUT')
             time.sleep(0.001)
         epoch = mmio.read(0x3838)
         require(epoch == ((pre_reset['0x3838'] + 1) & 0xFFFFFFFF),
-                'R3R4R1_SESSION_EPOCH_TRANSITION_INVALID')
+                'R3R4R2_SESSION_EPOCH_TRANSITION_INVALID')
         post_reset_error = mmio.read(0x383C)
         post_reset_cause = mmio.read(0x3840)
         nonfatal_mask = post_reset_error & 0x7
@@ -926,15 +926,15 @@
                        'POST_RESET_QUIESCENT')
         normalized_error = mmio.read(0x383C)
         require(normalized_error == 0,
-                'R3R4R1_POST_NORMALIZATION_ERROR_STATUS_NONZERO')
+                'R3R4R2_POST_NORMALIZATION_ERROR_STATUS_NONZERO')
         result['post_normalization_error_status'] = normalized_error
         result['last_completed_operation'] = 'POST_RESET_NORMALIZATION_PASS'
 
         status = mmio.read(0x3810)
         require(quiescent(mmio.read(0x380C), status) and
                 status & 0xC0 == 0xC0 and not (status & 0x800),
-                'R3R4R1_FINAL_PRE_ENABLE_BASELINE_FAILED')
-        require(mmio.read(0x3838) == epoch, 'R3R4R1_UNEXPECTED_EPOCH_CHANGE')
+                'R3R4R2_FINAL_PRE_ENABLE_BASELINE_FAILED')
+        require(mmio.read(0x3838) == epoch, 'R3R4R2_UNEXPECTED_EPOCH_CHANGE')
         baseline = mmio.snapshot('PRE_CAPTURE_BASELINE')
         result['snapshot_before'] = baseline
         result['last_completed_operation'] = 'COHERENT_BASELINE_SNAPSHOT_PASS'
@@ -946,15 +946,15 @@
             target=reader_worker,
             args=(proof['c2h'], str(ROOT / 'private'), contract, epoch,
                   parent_quiescent, messages),
-            name='r3r4-c2h-reader',
+            name='r3r4r2-c2h-reader',
         )
         child.start()
         ready_deadline = time.monotonic() + 5.0
         while 'READER_READY' not in result and time.monotonic() < ready_deadline:
             _drain_messages(messages, result, wait=0.1)
             require(child.is_alive() or 'READER_READY' in result,
-                    'R3R4R1_READER_EXITED_BEFORE_READY')
-        require('READER_READY' in result, 'R3R4R1_READER_READY_TIMEOUT')
+                    'R3R4R2_READER_EXITED_BEFORE_READY')
+        require('READER_READY' in result, 'R3R4R2_READER_READY_TIMEOUT')
         mmio.write(0x380C, 0x00000001, 'ENABLE_C2H',
                    'RESET_SNAPSHOT_PASS_READER_READY')
         stream_enabled = True
@@ -969,13 +969,13 @@
                     result['FIRST_RECORD_VALID'].get('valid'):
                 raise FailureError(
                     result['FIRST_RECORD_VALID'].get(
-                        'blocker', 'R3R4R1_FIRST_RECORD_ABI_VALIDATION_FAILED'))
+                        'blocker', 'R3R4R2_FIRST_RECORD_ABI_VALIDATION_FAILED'))
             if 'ERROR' in result:
                 raise GateError(result['ERROR'].get('blocker',
-                                                    'R3R4R1_READER_ERROR'))
-            require(child.is_alive(), 'R3R4R1_READER_EXITED_BEFORE_PRIMARY_TARGET')
+                                                    'R3R4R2_READER_ERROR'))
+            require(child.is_alive(), 'R3R4R2_READER_EXITED_BEFORE_PRIMARY_TARGET')
             require(time.monotonic() < primary_deadline,
-                    'R3R4R1_PRIMARY_2500_TIMEOUT')
+                    'R3R4R2_PRIMARY_2500_TIMEOUT')
         mmio.write(0x380C, 0x00000000, 'NORMAL_DISABLE',
                    'PRIMARY_2500_FULLY_ASSEMBLED_AND_WRITTEN')
         normal_disabled = True
@@ -987,7 +987,7 @@
         result['parent_quiescence_observations'] = observations
         result['parent_hardware_quiescence'] = 'PASS' if quiescence_pass else 'FAIL'
         require(quiescence_pass,
-                'R3R4R1_POST_DISABLE_HARDWARE_NOT_QUIESCENT')
+                'R3R4R2_POST_DISABLE_HARDWARE_NOT_QUIESCENT')
         parent_quiescent.set()
         result['last_completed_operation'] = 'PARENT_HARDWARE_QUIESCENCE_PASS'
 
@@ -998,24 +998,24 @@
                 _drain_messages(messages, result, wait=0.2)
                 break
         child.join(timeout=0.5)
-        require(not child.is_alive(), 'R3R4R1_READER_COOPERATIVE_EXIT_TIMEOUT')
+        require(not child.is_alive(), 'R3R4R2_READER_COOPERATIVE_EXIT_TIMEOUT')
         _drain_messages(messages, result, wait=0.2)
-        require('READER_QUIET' in result, 'R3R4R1_READER_QUIET_WINDOW_MISSING')
-        require('READER_EXITED' in result, 'R3R4R1_READER_RESULT_MISSING')
+        require('READER_QUIET' in result, 'R3R4R2_READER_QUIET_WINDOW_MISSING')
+        require('READER_EXITED' in result, 'R3R4R2_READER_RESULT_MISSING')
         reader_result = result['READER_EXITED']['reader_result']
         result['reader'] = reader_result
         require(reader_result.get('result') == 'PASS',
-                reader_result.get('blocker') or 'R3R4R1_READER_FAILED')
+                reader_result.get('blocker') or 'R3R4R2_READER_FAILED')
         require(reader_result.get('primary_records') == PRIMARY_TARGET,
-                'R3R4R1_PRIMARY_RECORD_COUNT_INVALID')
+                'R3R4R2_PRIMARY_RECORD_COUNT_INVALID')
         require(reader_result.get('drain_records', DRAIN_LIMIT + 1) <= DRAIN_LIMIT,
-                'R3R4R1_DRAIN_RECORD_LIMIT_EXCEEDED')
+                'R3R4R2_DRAIN_RECORD_LIMIT_EXCEEDED')
         require(reader_result.get('incomplete_trailing_bytes') == 0,
-                'R3R4R1_INCOMPLETE_TRAILING_BYTES_NONZERO')
+                'R3R4R2_INCOMPLETE_TRAILING_BYTES_NONZERO')
         require('FIRST_RECORD_DURABLE' in result and
                 'FIRST_RECORD_VALID' in result and
                 result['FIRST_RECORD_VALID'].get('valid'),
-                'R3R4R1_FIRST_RECORD_PERSISTENCE_OR_VALIDATION_MISSING')
+                'R3R4R2_FIRST_RECORD_PERSISTENCE_OR_VALIDATION_MISSING')
         result['last_completed_operation'] = 'READER_QUIET_EXIT_PASS'
 
         final_snapshot = mmio.snapshot('POST_DISABLE_POST_DRAIN_FINAL')
@@ -1024,11 +1024,11 @@
         final_error = mmio.read(0x383C)
         final_cause = mmio.read(0x3840)
         require(quiescent(final_control, final_status),
-                'R3R4R1_FINAL_STREAM_OR_DMA_NOT_QUIESCENT')
+                'R3R4R2_FINAL_STREAM_OR_DMA_NOT_QUIESCENT')
         if final_error != 0:
-            raise FailureError('R3R4R1_NEW_ERROR_STATUS_DURING_FINITE_CAPTURE')
+            raise FailureError('R3R4R2_NEW_ERROR_STATUS_DURING_FINITE_CAPTURE')
         require(final_snapshot['epoch'] == epoch,
-                'R3R4R1_UNEXPECTED_EPOCH_CHANGE')
+                'R3R4R2_UNEXPECTED_EPOCH_CHANGE')
         result.update(
             snapshot_after=final_snapshot, final_control=final_control,
             final_status=final_status, final_error_status=final_error,
@@ -1037,9 +1037,9 @@
 
         private = ROOT / 'private'
         validation = validate_capture(
-            contract, epoch, private / 'T34-primary-records.bin',
-            private / 'T34-drain-records.bin',
-            private / 'T34-incomplete-trailing.bin',
+            contract, epoch, private / 'T3T4-primary-records.bin',
+            private / 'T3T4-drain-records.bin',
+            private / 'T3T4-incomplete-trailing.bin',
         )
         result['validation'] = validation
         reconciliation = reconcile_counters(
@@ -1050,9 +1050,9 @@
         result['last_completed_operation'] = 'COUNTER_RECONCILIATION_PASS'
 
         frame = reconstruct_first_frame(
-            contract, [private / 'T34-primary-records.bin'],
-            private / 'T34-frame-1920x1080.uyvy',
-            private / 'T34-frame-1920x1080.png',
+            contract, [private / 'T3T4-primary-records.bin'],
+            private / 'T3T4-frame-1920x1080.uyvy',
+            private / 'T3T4-frame-1920x1080.png',
             armed_epoch=epoch, card_identity=proof['bdf'],
         )
         result['frame_reconstruction'] = frame
@@ -1088,7 +1088,7 @@
                     pass
             child.join(timeout=3.0)
         if child is not None and child.is_alive():
-            result['rollback'] = 'R3R4R1_ROLLBACK_UNSAFE_ACTIVE_DMA'
+            result['rollback'] = 'R3R4R2_ROLLBACK_UNSAFE_ACTIVE_DMA'
         elif messages is not None:
             try:
                 _drain_messages(messages, result, wait=0.2)
@@ -1098,8 +1098,8 @@
         if mmio is not None:
             mmio.close()
         require(bool(str(result.get('blocker', '')).strip()),
-                'R3R4R1_BLANK_BLOCKER_FORBIDDEN')
-        _replace_json(ROOT / 'logs/T34-result.json', result)
+                'R3R4R2_BLANK_BLOCKER_FORBIDDEN')
+        _replace_json(ROOT / 'logs/T3T4-result.json', result)
         print(json.dumps(result, indent=2), flush=True)
     return result
 
--- R3R4R1/capture_r3r4_selftest.py
+++ R3R4R2/capture_r3r4_selftest.py
@@ -1,5 +1,5 @@
 #!/usr/bin/env python3
-"""Offline hard-gate suite for the R3R4R1 capture architecture."""
+"""Offline hard-gate suite for the R3R4R2 capture architecture."""
 from __future__ import annotations
 
 import argparse
@@ -109,11 +109,11 @@
 def run_suite(root: Path, abi_path: Path) -> dict:
     expected_scripts = root / 'scripts'
     if Path(__file__).resolve().parent != expected_scripts.resolve():
-        raise RuntimeError('R3R4R1_SELFTEST_LOCATION_INVALID')
+        raise RuntimeError('R3R4R2_SELFTEST_LOCATION_INVALID')
     abi_bytes = abi_path.read_bytes()
     abi_hash = hashlib.sha256(abi_bytes).hexdigest().upper()
     if abi_hash != ABI_SHA256:
-        raise RuntimeError('R3R4R1_FROZEN_ABI_HASH_DRIFT')
+        raise RuntimeError('R3R4R2_FROZEN_ABI_HASH_DRIFT')
     contract = AbiContract.load(abi_path)
     suite_root = root / 'artifacts' / 'offline-selftest'
     suite_root.mkdir(parents=True, exist_ok=False)
@@ -137,8 +137,8 @@
     first_blob = records[0]
     first_payload = first_blob[contract.payload_first:contract.padding_first]
     assert success['first_record_durable'] and success['first_record_valid']
-    assert (success_dir / 'T34-first-record.bin').read_bytes() == first_blob
-    assert (success_dir / 'T34-first-payload.bin').read_bytes() == first_payload
+    assert (success_dir / 'T3T4-first-record.bin').read_bytes() == first_blob
+    assert (success_dir / 'T3T4-first-payload.bin').read_bytes() == first_payload
     assert success['first_record_sha256'] == hashlib.sha256(first_blob).hexdigest().upper()
     assert success['first_payload_sha256'] == hashlib.sha256(first_payload).hexdigest().upper()
     assert events['PRIMARY_TARGET_REACHED']['monotonic_ns'] < \
@@ -146,8 +146,8 @@
     cases['FIRST_RECORD_PERSISTENCE_PASS'] = 'PASS'
     details['first_record_async'] = True
 
-    primary = success_dir / 'T34-primary-records.bin'
-    drain = success_dir / 'T34-drain-records.bin'
+    primary = success_dir / 'T3T4-primary-records.bin'
+    drain = success_dir / 'T3T4-drain-records.bin'
     primary_bytes = primary.read_bytes()
     drain_bytes = drain.read_bytes()
     expected_primary_hash = hashlib.sha256(
@@ -170,7 +170,7 @@
     assert success['drain_records'] == 7
     assert success['drain_bytes'] == 28_672
     assert success['incomplete_trailing_bytes'] == 0
-    assert (success_dir / 'T34-incomplete-trailing.bin').stat().st_size == 0
+    assert (success_dir / 'T3T4-incomplete-trailing.bin').stat().st_size == 0
     assert success['primary_sha256'] == expected_primary_hash
     assert success['drain_sha256'] == expected_drain_hash
     assert hashlib.sha256(primary_bytes).hexdigest().upper() == \
@@ -223,7 +223,7 @@
         quiet_since, done = capture.quiet_window_update(
             parent_pass, data, now, quiet_since, 1.0)
         completions.append(done)
-    assert completions == [False, False, False, False, False, False, True]
+    assert completions == [False, False, False, False, False, True, True]
     cases['PARENT_QUIESCENCE_HANDSHAKE_PASS'] = 'PASS'
     details['timed_no_data_reads_emulated'] = 4
     details['delayed_parent_quiescence_emulated'] = True
@@ -245,8 +245,8 @@
             traceback_text=traceback.format_exc(),
         )
     assert failure_state is not None
-    assert (failure_dir / 'T34-primary-records.bin').stat().st_size == 7 * 4096
-    assert (failure_dir / 'T34-incomplete-trailing.bin').stat().st_size == 333
+    assert (failure_dir / 'T3T4-primary-records.bin').stat().st_size == 7 * 4096
+    assert (failure_dir / 'T3T4-incomplete-trailing.bin').stat().st_size == 333
     assert failure_state['primary_records'] == 7
     assert failure_state['incomplete_trailing_bytes'] == 333
     cases['FAILURE_PRESERVES_RAW_DATA_PASS'] = 'PASS'
@@ -260,14 +260,14 @@
 
     frame = reconstruct_first_frame(
         contract, [primary],
-        suite_root / 'T34-frame-1920x1080.uyvy',
-        suite_root / 'T34-frame-1920x1080.png',
+        suite_root / 'T3T4-frame-1920x1080.uyvy',
+        suite_root / 'T3T4-frame-1920x1080.png',
         armed_epoch=77, card_identity='SELFTEST',
     )
     assert frame['result'] == 'PASS'
     assert frame['raw_frame_bytes'] == 4_147_200
     assert frame['width'] == 1920 and frame['height'] == 1080
-    assert (suite_root / 'T34-frame-1920x1080.png').read_bytes()[:8] == \
+    assert (suite_root / 'T3T4-frame-1920x1080.png').read_bytes()[:8] == \
         b'\x89PNG\r\n\x1a\n'
     cases['COMPLETE_FRAME_RECONSTRUCTION_PASS'] = 'PASS'
     details['frame'] = frame
@@ -298,7 +298,7 @@
     details['control_event_count'] = len(all_events)
 
     if tuple(cases) != CASE_NAMES or any(value != 'PASS' for value in cases.values()):
-        raise RuntimeError('R3R4R1_CAPTURE_TOOL_HARD_GATE_FAILED')
+        raise RuntimeError('R3R4R2_CAPTURE_TOOL_HARD_GATE_FAILED')
     result = {
         'task': capture.TASK,
         'result': 'PASS',
@@ -313,15 +313,15 @@
         'parent_owned_mmio': True,
         'parent_owned_quiescence': True,
     }
-    output = root / 'artifacts' / 'G2B_HW0_PRODUCT_R3R4R1_CAPTURE_TOOL_SELFTEST.json'
+    output = root / 'artifacts' / 'G2B_HW0_PRODUCT_R3R4R2_CAPTURE_TOOL_SELFTEST.json'
     with output.open('x', encoding='utf-8', newline='\n') as handle:
         json.dump(result, handle, indent=2)
         handle.write('\n')
         handle.flush()
         os.fsync(handle.fileno())
     write_markdown(
-        root / 'artifacts' / 'G2B_HW0_PRODUCT_R3R4R1_CAPTURE_TOOL_SELFTEST.md',
-        'G2B-HW0-PRODUCT-R3R4R1 capture-tool offline self-test',
+        root / 'artifacts' / 'G2B_HW0_PRODUCT_R3R4R2_CAPTURE_TOOL_SELFTEST.md',
+        'G2B-HW0-PRODUCT-R3R4R2 capture-tool offline self-test',
         [(name, cases[name]) for name in CASE_NAMES] +
         [('Hardware access', 'NO'), ('DUT connections', '0')],
     )
@@ -331,7 +331,7 @@
 def architecture_audit(root: Path, result: dict) -> dict:
     capture_path = root / 'scripts' / 'capture_r3r4.py'
     frame_path = root / 'scripts' / 'frame_reconstruct_r3r4.py'
-    helper_path = root / 'scripts' / 'Invoke-R3R4R1DutConnection.ps1'
+    helper_path = root / 'scripts' / 'Invoke-R3R4R2DutConnection.ps1'
     source = capture_path.read_text(encoding='utf-8')
     reader_source = inspect.getsource(capture.reader_worker)
     checks = {
@@ -341,7 +341,7 @@
         'SPAWN_PREVENTS_MMIO_FD_INHERITANCE': "mp.get_context('spawn')" in source,
         'ZERO_RAW_RECORD_CONTROL_IPC': result['raw_record_control_ipc'] is False,
         'ASYNC_FIRST_RECORD_PERSISTER':
-            'r3r4r1-first-record-persister' in source and 'threading.Thread' in source,
+            'r3r4r2-first-record-persister' in source and 'threading.Thread' in source,
         'PRIMARY_TARGET_2500': capture.PRIMARY_TARGET == 2500,
         'PRIMARY_BYTES_10240000': capture.PRIMARY_BYTES == 10_240_000,
         'DRAIN_LIMIT_512': capture.DRAIN_LIMIT == 512,
@@ -351,7 +351,7 @@
         'FROZEN_ABI_HASH': result['abi_sha256'] == ABI_SHA256,
     }
     if not all(checks.values()):
-        raise RuntimeError('R3R4R1_CAPTURE_TOOL_HARD_GATE_FAILED:' +
+        raise RuntimeError('R3R4R2_CAPTURE_TOOL_HARD_GATE_FAILED:' +
                            json.dumps(checks, sort_keys=True))
     hashes = {}
     for path in (capture_path, frame_path, helper_path,
@@ -368,15 +368,15 @@
         'selftests_passed': result['passed'],
         'selftests_total': result['total'],
     }
-    with (root / 'artifacts' / 'G2B_HW0_PRODUCT_R3R4R1_CAPTURE_TOOL_AUDIT.json').open(
+    with (root / 'artifacts' / 'G2B_HW0_PRODUCT_R3R4R2_CAPTURE_TOOL_AUDIT.json').open(
             'x', encoding='utf-8', newline='\n') as handle:
         json.dump(audit, handle, indent=2)
         handle.write('\n')
         handle.flush()
         os.fsync(handle.fileno())
     write_markdown(
-        root / 'artifacts' / 'G2B_HW0_PRODUCT_R3R4R1_CAPTURE_TOOL_AUDIT.md',
-        'G2B-HW0-PRODUCT-R3R4R1 capture-tool architecture audit',
+        root / 'artifacts' / 'G2B_HW0_PRODUCT_R3R4R2_CAPTURE_TOOL_AUDIT.md',
+        'G2B-HW0-PRODUCT-R3R4R2 capture-tool architecture audit',
         [(name, 'PASS' if value else 'FAIL') for name, value in checks.items()] +
         [('Offline self-tests', f"{result['passed']}/{result['total']}")],
     )
@@ -396,13 +396,13 @@
     except BaseException as exc:
         failure = {
             'task': capture.TASK, 'result': 'BLOCKED',
-            'blocker': 'R3R4R1_CAPTURE_TOOL_HARD_GATE_FAILED',
+            'blocker': 'R3R4R2_CAPTURE_TOOL_HARD_GATE_FAILED',
             'exception_type': type(exc).__name__,
             'exception_repr': repr(exc),
             'traceback': traceback.format_exc(),
             'hardware_access': False, 'dut_connections': 0,
         }
-        path = root / 'artifacts' / 'G2B_HW0_PRODUCT_R3R4R1_CAPTURE_TOOL_FAILURE.json'
+        path = root / 'artifacts' / 'G2B_HW0_PRODUCT_R3R4R2_CAPTURE_TOOL_FAILURE.json'
         path.write_text(json.dumps(failure, indent=2) + '\n', encoding='utf-8')
         print(json.dumps(failure, indent=2))
         return 1
--- R3R4R1/frame_reconstruct_r3r4.py
+++ R3R4R2/frame_reconstruct_r3r4.py
@@ -41,7 +41,7 @@
                     break
                 if len(blob) != RECORD_BYTES:
                     raise ReconstructionError(
-                        f"R3R4R1_FRAME_INPUT_TRAILING_BYTES:{path}:{len(blob)}"
+                        f"R3R4R2_FRAME_INPUT_TRAILING_BYTES:{path}:{len(blob)}"
                     )
                 yield blob
 
@@ -54,7 +54,7 @@
     expected = width * height * 2
     if len(raw) != expected:
         raise ReconstructionError(
-            f"R3R4R1_UYVY_SIZE_INVALID:{len(raw)}:{expected}"
+            f"R3R4R2_UYVY_SIZE_INVALID:{len(raw)}:{expected}"
         )
     stride = width * 2
     for line in range(height):
@@ -110,27 +110,27 @@
         records_examined += 1
         if not assessment.structurally_valid or assessment.session_fatal:
             raise ReconstructionError(
-                "R3R4R1_FRAME_RECORD_INVALID:" +
+                "R3R4R2_FRAME_RECORD_INVALID:" +
                 ";".join(assessment.errors + assessment.discontinuity_reasons)
             )
         if assessment.discontinuity_reasons:
             raise ReconstructionError(
-                "R3R4R1_FRAME_DISCONTINUITY:" +
+                "R3R4R2_FRAME_DISCONTINUITY:" +
                 ";".join(assessment.discontinuity_reasons)
             )
         completed = assembler.push(assessment)
         if completed is not None:
             break
     if completed is None:
-        raise ReconstructionError("R3R4R1_COMPLETE_FRAME_NOT_FOUND")
+        raise ReconstructionError("R3R4R2_COMPLETE_FRAME_NOT_FOUND")
 
     expected_size = contract.pixels_per_line * contract.lines_per_frame * 2
     if len(completed.raw_uyvy) != expected_size:
         raise ReconstructionError(
-            f"R3R4R1_FRAME_SIZE_INVALID:{len(completed.raw_uyvy)}:{expected_size}"
+            f"R3R4R2_FRAME_SIZE_INVALID:{len(completed.raw_uyvy)}:{expected_size}"
         )
     if completed.line_sequences != tuple(range(contract.lines_per_frame)):
-        raise ReconstructionError("R3R4R1_FRAME_LINE_SET_INVALID")
+        raise ReconstructionError("R3R4R2_FRAME_LINE_SET_INVALID")
 
     raw_sha = _fsync_file(raw_output, completed.raw_uyvy)
     png_sha = write_png_from_uyvy(
--- R3R4R1/Invoke-R3R4R1DutConnection.ps1
+++ R3R4R2/Invoke-R3R4R2DutConnection.ps1
@@ -11,8 +11,8 @@
 Set-StrictMode -Version Latest
 $ErrorActionPreference = 'Stop'
 
-$run = 'C:\FPGA\G2B_HW0_PRODUCT_R3R4R1_20260907T050126Z'
-$linuxRun = '/home/vcdeagent1/vcde_artifacts/g2b_hw0_product_r3r4r1/20260907T050126Z'
+$run = 'C:\FPGA\G2B_HW0_PRODUCT_R3R4R2_20260907T071912Z'
+$linuxRun = '/home/vcdeagent1/vcde_artifacts/g2b_hw0_product_r3r4r2/20260907T071912Z'
 $private = Join-Path $run 'private'
 $credentialTemp = Join-Path $private 'credential-temp'
 $receipt = Join-Path $run ('logs\connection-' + $ReceiptName + '.json')
@@ -29,14 +29,14 @@
   if (-not $acl.AreAccessRulesProtected -or $rules.Count -ne 1 -or
       $rules[0].IdentityReference.Value -cne $sid.Value -or
       $rules[0].AccessControlType -ne 'Allow') {
-    throw 'R3R4R1_PRIVATE_ACL_INVALID'
+    throw 'R3R4R2_PRIVATE_ACL_INVALID'
   }
 }
 
 if ([IO.Path]::GetFullPath($PSScriptRoot) -cne (Join-Path $run 'scripts')) {
-  throw 'R3R4R1_HELPER_LOCATION_INVALID'
-}
-if (Test-Path -LiteralPath $receipt) { throw 'R3R4R1_RECEIPT_EXISTS' }
+  throw 'R3R4R2_HELPER_LOCATION_INVALID'
+}
+if (Test-Path -LiteralPath $receipt) { throw 'R3R4R2_RECEIPT_EXISTS' }
 if (-not (Test-Path -LiteralPath $credentialTemp -PathType Container)) {
   New-Item -ItemType Directory -Path $credentialTemp | Out-Null
   $acl = [Security.AccessControl.DirectorySecurity]::new()
@@ -49,10 +49,10 @@
 Assert-PrivateAcl $private
 Assert-PrivateAcl $credentialTemp
 if (@(Get-ChildItem -LiteralPath $credentialTemp -Force).Count) {
-  throw 'R3R4R1_CREDENTIAL_TEMP_NOT_EMPTY'
+  throw 'R3R4R2_CREDENTIAL_TEMP_NOT_EMPTY'
 }
 if ((Get-FileHash -LiteralPath $plink).Hash -cne $plinkHash) {
-  throw 'R3R4R1_PLINK_HASH_MISMATCH'
+  throw 'R3R4R2_PLINK_HASH_MISMATCH'
 }
 
 $pw = $null
@@ -75,7 +75,7 @@
   foreach ($line in [IO.File]::ReadAllLines('C:\FPGA\VCDE-DUT-1.txt')) {
     if (-not $line.Trim() -or $line.Trim().StartsWith('#')) { continue }
     if ($line -notmatch '^\s*([^:=]+?)\s*[:=]\s*(.*?)\s*$') {
-      throw 'R3R4R1_CREDENTIAL_FORMAT'
+      throw 'R3R4R2_CREDENTIAL_FORMAT'
     }
     $label = ($Matches[1] -replace '[^A-Za-z0-9]','').ToLowerInvariant()
     $val = $Matches[2]
@@ -83,31 +83,31 @@
       {$_ -in @('ip','adresip')} {'ip'}
       {$_ -in @('user','username','uzytkownik','sudouser')} {'user'}
       {$_ -in @('password','haslo')} {'password'}
-      default { throw 'R3R4R1_CREDENTIAL_FIELD' }
-    }
-    if ($fields.ContainsKey($key)) { throw 'R3R4R1_DUPLICATE_CREDENTIAL_FIELD' }
+      default { throw 'R3R4R2_CREDENTIAL_FIELD' }
+    }
+    if ($fields.ContainsKey($key)) { throw 'R3R4R2_DUPLICATE_CREDENTIAL_FIELD' }
     $fields[$key] = $val
   }
   if ($fields.Count -ne 3 -or $fields.ip -cne $ip -or $fields.user -cne $login) {
-    throw 'R3R4R1_CREDENTIAL_IDENTITY'
+    throw 'R3R4R2_CREDENTIAL_IDENTITY'
   }
   $pw = [string]$fields.password
   $fields.Clear()
   $val = $null
   $line = $null
-  if ([string]::IsNullOrWhiteSpace($pw)) { throw 'R3R4R1_EMPTY_CREDENTIAL' }
+  if ([string]::IsNullOrWhiteSpace($pw)) { throw 'R3R4R2_EMPTY_CREDENTIAL' }
 
   if ($mode -eq 'UPLOAD') {
     $resolvedUpload = [IO.Path]::GetFullPath($UploadFile)
     if (-not $resolvedUpload.StartsWith((Join-Path $run 'scripts') + '\',[StringComparison]::OrdinalIgnoreCase)) {
-      throw 'R3R4R1_UPLOAD_SOURCE_BOUNDARY'
+      throw 'R3R4R2_UPLOAD_SOURCE_BOUNDARY'
     }
     if (-not (Test-Path -LiteralPath $resolvedUpload -PathType Leaf)) {
-      throw 'R3R4R1_UPLOAD_SOURCE_MISSING'
+      throw 'R3R4R2_UPLOAD_SOURCE_MISSING'
     }
     $escapedRun = [Regex]::Escape($linuxRun)
     if ($RemotePath -notmatch ('^' + $escapedRun + '/scripts/[A-Za-z0-9._-]+$')) {
-      throw 'R3R4R1_UPLOAD_DESTINATION_BOUNDARY'
+      throw 'R3R4R2_UPLOAD_DESTINATION_BOUNDARY'
     }
     $uploadBytes = (Get-Item -LiteralPath $resolvedUpload).Length
     $uploadSha256 = (Get-FileHash -LiteralPath $resolvedUpload).Hash
@@ -123,7 +123,7 @@
 
   $temp = Join-Path $credentialTemp ([Guid]::NewGuid().ToString('N') + '.credential.tmp')
   if (-not [IO.Path]::GetFullPath($temp).StartsWith($credentialTemp + '\',[StringComparison]::OrdinalIgnoreCase)) {
-    throw 'R3R4R1_TEMP_BOUNDARY'
+    throw 'R3R4R2_TEMP_BOUNDARY'
   }
   $stream = [IO.File]::Open($temp,'CreateNew','Write','None')
   $stream.Dispose()
@@ -146,9 +146,9 @@
   $argsList = @('-batch','-ssh','-4','-P','22','-l',$login,'-pwfile',$temp,
                 '-noagent','-noshare','-hostkey',$hostKey,'-T',$ip,$remote)
   for ($i=0; $i -lt $argsList.Count; $i++) {
-    if ($argsList[$i] -ceq '-pw') { throw 'R3R4R1_SECRET_ARGUMENT' }
+    if ($argsList[$i] -ceq '-pw') { throw 'R3R4R2_SECRET_ARGUMENT' }
     if ($i -ne 6 -and $argsList[$i].Contains($pw,[StringComparison]::Ordinal)) {
-      throw 'R3R4R1_SECRET_ARGUMENT'
+      throw 'R3R4R2_SECRET_ARGUMENT'
     }
     [void]$psi.ArgumentList.Add($argsList[$i])
   }
@@ -161,7 +161,7 @@
   $proc = [Diagnostics.Process]::new()
   $proc.StartInfo = $psi
   $started = $proc.Start()
-  if (-not $started) { throw 'R3R4R1_PROCESS_START' }
+  if (-not $started) { throw 'R3R4R2_PROCESS_START' }
   $outTask = $proc.StandardOutput.ReadToEndAsync()
   $errTask = $proc.StandardError.ReadToEndAsync()
   if ($Sudo) {
@@ -176,16 +176,16 @@
     $proc.Kill($true)
     $proc.WaitForExit()
     $exitCode = 124
-    throw 'R3R4R1_CONNECTION_TIMEOUT'
+    throw 'R3R4R2_CONNECTION_TIMEOUT'
   }
   $stdout = $outTask.GetAwaiter().GetResult()
   $stderr = $errTask.GetAwaiter().GetResult()
   $exitCode = $proc.ExitCode
 } catch {
-  $problem = if ($_.Exception.Message -like 'R3R4R1_*') {
+  $problem = if ($_.Exception.Message -like 'R3R4R2_*') {
     $_.Exception.Message
   } else {
-    'R3R4R1_SANITIZED_LOCAL_EXCEPTION'
+    'R3R4R2_SANITIZED_LOCAL_EXCEPTION'
   }
 } finally {
   if ($proc) {
@@ -202,11 +202,11 @@
   }
 }
 
-if (-not $deleted) { $exitCode=126; $problem='R3R4R1_CREDENTIAL_DELETE_FAILED' }
+if (-not $deleted) { $exitCode=126; $problem='R3R4R2_CREDENTIAL_DELETE_FAILED' }
 $remaining = @(Get-ChildItem -LiteralPath $credentialTemp -Force).Count
-if ($remaining -ne 0) { $exitCode=127; $problem='R3R4R1_CREDENTIAL_REMNANT' }
+if ($remaining -ne 0) { $exitCode=127; $problem='R3R4R2_CREDENTIAL_REMNANT' }
 $result = [ordered]@{
-  task='G2B-HW0-PRODUCT-R3R4R1'
+  task='G2B-HW0-PRODUCT-R3R4R2'
   mode=$mode
   helper=$PSCommandPath
   helper_sha256=(Get-FileHash -LiteralPath $PSCommandPath).Hash
@@ -215,7 +215,7 @@
   ip=$ip
   host_key_pinned=$true
   credential_in_process_arguments=$false
-  temporary_path_class='R3R4R1_PRIVATE_CREDENTIAL_TEMP'
+  temporary_path_class='R3R4R2_PRIVATE_CREDENTIAL_TEMP'
   temporary_created=$created
   temporary_deleted=$deleted
   credential_temp_remaining=$remaining
@@ -231,4 +231,4 @@
 [IO.File]::WriteAllText($receipt,$json + "`n",[Text.UTF8Encoding]::new($false))
 $pw = $null
 Write-Output $json
-if ($exitCode -ne 0 -or $problem) { throw 'R3R4R1_CONNECTION_FAILED_SEE_RECEIPT' }
+if ($exitCode -ne 0 -or $problem) { throw 'R3R4R2_CONNECTION_FAILED_SEE_RECEIPT' }
