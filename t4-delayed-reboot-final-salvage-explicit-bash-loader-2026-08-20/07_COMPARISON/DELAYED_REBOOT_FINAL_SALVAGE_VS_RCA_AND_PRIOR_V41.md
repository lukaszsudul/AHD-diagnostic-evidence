# Comparison

The original delayed-reboot row remains infrastructure-invalid and is not replaced. The salvaged row is appended as a separate, infrastructure-valid functional FAIL. It matches the broad prior v41 failure class—completed autoinit with NACKs and no SAV/frame activity—but its exact error location is recorded separately and is not called identical to other tuples. RC-A remains the 3/3 passing current-hardware control.

