# CONT1R2 Scope

CONT1R2 isolated the previously recovered SCAN1 NACK and ran a read-only integrity qualification. A recovered NACK appeared at qualification scan 7, so the 10,000-scan zero-error gate failed closed. The bounded same-cadence characterization continued until the global 10-event limit; spaced cadence became safely not applicable. No functional NVP write, camera gate, baseline, slice action, mode, EQ, or capture was reached.

Private raw snapshots remain local. This public set contains only manifests, hashes, per-scan summaries, recovered-event rows, decisions, and cleanup receipts.
