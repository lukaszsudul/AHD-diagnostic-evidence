# W4 handoff: blocked at C4 resource gate

No C4 bitstream exists. W4 remains a separately authorized hardware task and cannot consume this candidate. The fourth and final authorized source revision stopped at `POST_OPT_LUT_GATE_FAILED:20540`; a fifth source revision needs a new explicit Owner decision.

For a later qualifying image, W4 must use the same exact bitstream, driver identity and initialization for OFF and ON, with the previously governed scan/time budget, no recovered-NACK count limit and unchanged hard stops. Confirm endpoint, boot, exact PCI BDF, driver/module hash, node ancestry and register identity afresh before any hardware action. The 10,000-clean-scan gate remains in force; an earlier demonstrator requires a separate explicit decision. New W3b telemetry is not a prerequisite. A bitstream alone would not prove a complete real camera frame.

This C4 task performed no DUT contact, programming, hardware scan, camera capture or W4 activity.
