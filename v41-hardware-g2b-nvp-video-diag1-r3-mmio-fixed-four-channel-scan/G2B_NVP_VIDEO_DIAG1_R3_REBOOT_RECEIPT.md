# Warm reboot receipt

PASS. One graceful Ubuntu warm reboot was delivered after programming. The first bounded reconnect probe timed out and the second connected; this was reconnect polling, not a second reboot. No boot-ID comparison was used for requalification and no power-cycle occurred.
