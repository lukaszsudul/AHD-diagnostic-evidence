# Owner-authorized scope

The task text in the user-provided CONT1R3 attachment authorizes a minimal first-attempt cause/temporal/positional diagnostic RTL delta, offline simulations and tests, one fresh sign-off build, one exact volatile-SRAM programming, product-equivalent autoinit, at most one warm reboot, one read-only control scan and exactly 1,000 subsequent read-only characterization scans. Safe cleanup and append-only evidence publication are authorized.

No experimental functional NVP writes, camera/slice/MODE1/EQ work, generic host I2C, DMA capture, Flash operation, power-cycle, cadence sweep or 10,000-scan qualification are authorized. The retired DUT endpoint must not be contacted. Hard scan, telemetry, identity, rollback/restore or concurrent-activity gates stop the experiment.
