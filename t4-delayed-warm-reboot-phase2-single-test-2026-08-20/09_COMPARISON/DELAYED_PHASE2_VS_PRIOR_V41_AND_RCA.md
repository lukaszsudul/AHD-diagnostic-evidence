# Delayed Phase-2 comparison

The three RC-A controls remain valid passes and historical v41 samples remain valid functional failures. The current sample met its programming, DONE, delay, reboot, new-boot and endpoint geometry gates, but the pinned XDMA runtime/user node did not return. Therefore the current row is infrastructure-invalid and cannot be compared functionally with either class. Historical DONE-to-reboot timing remains `UNKNOWN_NOT_RECORDED`.
