# NVP baseline

`NOT_RUN_INFRASTRUCTURE_PCIE_HOST_GATE`. The required pinned XDMA driver and user node were absent after reboot. No AXI-Lite read/write, NVP access, capture-arm operation or DMA was attempted. No functional inference is permitted.
