# NEXT HARDWARE HANDOFF — documentation only

CONT1R3R3 generated and offline-accepted the private diagnostic firmware `C:\FPGA\G2B_NVP_CAMERA_ACQ1_COMPAT0_R2R1_CONT1R3R3_20260916T185629Z\firmware\AHD_v41_CONT1R3R3_DIAGNOSTIC.bit`, SHA-256 `CD80C84E17467BCB03DEE58DC7FF64D50FD2CB6E5C409E21F871C3E05052BA09`. It has **not** been deployed. Runtime implementation identity remains `CONT1R3R1_BRAM_TELEMETRY`; CONT1R3R3 is an artifact-generation task name, not a new RTL version.

For a future separately authorized task, the sole DUT endpoint authority is `10.132.1.111:22`; `192.168.1.57:22` is not a fallback. The Owner reported on 2026-09-16 that a warm reboot **may** have occurred and reported no other modifications. No reboot time/count or current boot ID was supplied. Current FPGA runtime, NVP state, driver state, locks/tasks and XDMA nodes were not surveyed. The prior exact volatile image is neither guaranteed present nor presumed absent. Do not concatenate measurements across boot sessions.

A later bounded live survey must first verify the established SSH host-key authority and stable hostname/machine identity (`VCDE-DUT-1`, machine-id `0e90f50d9465492b80258da5658446f8`), then record actual boot ID and inspect locks, tasks/processes, driver and XDMA nodes. Obtain runtime identity through a separately authorized safe path before scanner activity. A changed boot ID alone is compatible with the Owner's statement and requires a new boot-scoped baseline. Changed machine identity/host key, a conflicting task, unsafe bus/runtime state or unexplained modification is a stop condition. Do not disable host-key checks, infer SRAM loss from a missing device, or reboot/reprogram as discovery.

This handoff authorizes no automatic driver loading, cleanup, NVP restoration, reboot or programming. A later deployment task must explicitly authorize such actions and verify this exact firmware hash before use. The firmware preserves first-attempt error/position/time observations but is **not** a proven NACK fix. The planned 1,000-scan causal campaign and separate 10,000-scan zero-error requirement are both **NOT_RUN**; neither is a genuine camera frame. No camera, slice, MODE1, EQ or capture operation occurred here.

Next practical step: authorize a bounded live DUT survey and governed activation/testing of this exact generated image, using the possible-warm-reboot assumption.
