# CONT1R3R3 exact signed-DCP bitstream receipt

Engineering disposition: **PASS for offline firmware generation only**. The real, private firmware file is `C:\FPGA\G2B_NVP_CAMERA_ACQ1_COMPAT0_R2R1_CONT1R3R3_20260916T185629Z\firmware\AHD_v41_CONT1R3R3_DIAGNOSTIC.bit`, 2,192,144 bytes, SHA-256 `CD80C84E17467BCB03DEE58DC7FF64D50FD2CB6E5C409E21F871C3E05052BA09`. Two independent full reads after generation agreed. The file itself is **not** in this public repository.

The only generation input was a private byte-identical copy of `CONT1R3R1_SIGNED_OFF_ROUTED.dcp`, 17,469,233 bytes, SHA-256 `C09145B1397E2A8DC972E435E755917C9104A5FDA2E5EF297FE4628EF9672CC2`. The original and copy were measured before and after generation; size and SHA-256 remained exact. This links the output to accepted CONT1R3R2 closure commit `9be3c9f023356f9dd04a4e42c2cc5944c97c211b` and frozen source commit/tree `09cd7cbb426027acaefd0cf3989579b80a451f3a` / `c6be008ddc387c1f43eefa35d4fed0e2ccb968db`. Prior A/B sign-off is inherited by exact identity, not rerun as a design implementation.

Vivado 2025.2 SW build 6299465 freshly opened that DCP as stored, reporting part `xc7a35tcsg325-2`, top `ahd_capture_top_xdma`, and 38,667/38,667 fully routed nets with zero routing errors. Fresh default DRC had zero errors, zero critical warnings and the inherited 24 ordinary-warning vector: IOSR-1 ×2, PDCN-1569 ×1, REQP-1839 ×12, REQP-1840 ×8, RTSTAT-10 ×1. One plain `write_bitstream <fresh-absolute-.bit-path>` ran its required DRC with zero errors; Vivado reported `Bitgen Completed Successfully` and returned exit code 0. No `-force`, `-quiet`, waiver, severity downgrade, configuration-property edit, XDC replay, checkpoint write or implementation operation was used.

Read-only output validation found the standard length-prefixed Xilinx `.bit` container: design field `ahd_capture_top_xdma`, normalized header part `7a35tcsg325`, 132-byte header, declared and actual configuration payload both 2,192,012 bytes, aligned sync word `AA995566` at payload offset 48, and no truncation or trailing bytes. The header does not encode/prove speed grade or circuit behavior; accepted source/DCP/tool provenance binds the speed grade. Full-image classification follows the plain `write_bitstream` command on the implemented design with no partial/incremental options, not file size alone.

The retained source/DCP implementation identity is `CONT1R3R1_BRAM_TELEMETRY`, magic/version `0x52335231 / 0x00010000`. It was not measured on the DUT. No firmware deployment, scanner campaign, NVP write or genuine camera-frame acquisition occurred. Publication/read-back disposition: **PENDING** until the new evidence commit is remotely verified.
