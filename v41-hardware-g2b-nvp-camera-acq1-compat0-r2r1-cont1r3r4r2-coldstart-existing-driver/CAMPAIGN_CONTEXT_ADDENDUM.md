# CONT1R3R4R2 characterization context addendum

The Owner attested one normal whole-DUT shutdown and physical cold start via
`DUT_COLD_START_COMPLETE`. Rail removal was not independently measured.

The measurement boot is `e9656f6c-dd41-4500-95cc-9dc90ef9b69d`, after one
separately recorded deployment warm reboot following exact SRAM programming.
This starting condition differs from CONT1R2 and must not be described as an
identical repeat.

The preregistered analysis remains unchanged: SHA-256
`18CDD7FBC67ABDB799475CE509BC686C3A2CD0E7153BC4E44910ABC809B5DF8C`.
No thresholds, bins, retry rule, scan cadence, or scientific questions are
changed by this addendum. The intended session consists of one control scan
plus 1,000 consecutive campaign scans. This task authorizes no camera capture
and cannot produce a real frame.
