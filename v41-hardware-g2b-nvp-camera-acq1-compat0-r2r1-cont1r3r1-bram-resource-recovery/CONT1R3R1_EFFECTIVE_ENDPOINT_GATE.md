# Endpoint authority

Owner DOC0 and META-9 established `10.132.1.111:22` as the sole operational DUT SSH endpoint; `192.168.1.57:22` is superseded. The local closed-bundle endpoint JSON resolves only to the new endpoint with no fallback. The SSH host-key/effective DUT connection sub-gate is NOT_REACHED because offline sign-off failed. No address was probed.
