# Scope and stop

Engineering stopped before bitstream generation because the saved DCP did not reproduce the required canonical XDC identity after reopening. The volatile FPGA state was not changed. The 1,000-scan campaign and all hardware gates are NOT_REACHED.
