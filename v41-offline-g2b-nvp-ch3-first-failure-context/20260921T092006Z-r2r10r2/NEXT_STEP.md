# Next step

A separate Owner-authorized hardware session may recover and activate the exact pinned image, establish a fresh empty record, run PREPARE, run exactly one APPLY_A, and persist the record before cleanup. Any SCL/SDA trace mode and bounded CH3 synchronization check must be included in that future authorization. APPLY_B, DMA and capture are not admitted by this release.