# Owner prompt preservation note

The authoritative owner prompt is the thread message headed:

`v41 NVP R1e — extended observability build`

Task identity:

`V41_NVP_R1E_EXTENDED_OBSERVABILITY_PAIRED_AB_R1`

Authorization identity:

`OWNER_STANDING_AUTHORIZATION=GRANTED`

The complete verbatim prompt remains immutable in the originating Codex thread. The runtime did not expose a safe transcript-export primitive, so this local evidence copy records the authoritative thread identity and does not claim to be a byte-for-byte transcript. No prompt instruction was relaxed: the one-build/no-correction hard stop was applied after the report-script failure.
