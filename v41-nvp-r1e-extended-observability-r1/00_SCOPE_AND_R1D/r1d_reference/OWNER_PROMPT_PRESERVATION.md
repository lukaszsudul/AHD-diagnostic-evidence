# Owner master-prompt preservation

The originating Codex conversation contains the verbatim Owner prompt headed:

`CODEX MASTER PROMPT — v41 NVP R1d — high-statistics post-init address-ACK probe`

Task identity:

`V41_NVP_ADDRESS_ACK_PROBE_50K_25K_R1D`

Coordination revision:

`R2_LITEPCIE_ISOLATION`

This local execution environment does not expose a raw-message export API, so
the very large verbatim prompt could not be mechanically exported without
manually re-transcribing it. The originating conversation is therefore the
authoritative verbatim copy. This evidence tree preserves the implemented
requirements, identities, ledgers, source diff, simulations, build artifacts,
and final report. No hardware-window statement was present in the Owner prompt;
the later hardware sections did not constitute that separate assignment.

