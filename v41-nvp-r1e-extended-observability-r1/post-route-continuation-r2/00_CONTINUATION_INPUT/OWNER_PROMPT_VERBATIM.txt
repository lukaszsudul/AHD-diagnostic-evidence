CODEX MASTER PROMPT

v41 NVP R1e — namespace-correct post-route continuation R2

One read-only harness preflight, one write-capable continuation, one R1e bit,

full Arm A and full Arm B, one authoritative final report

FULL OWNER PRE-AUTHORIZATION — NO INTERACTIVE CONFIRMATIONS

```text
PROJECT:
    AHD Capture Card

TASK:
    V41_NVP_R1E_NAMESPACE_CORRECT_POST_ROUTE_CONTINUATION_AND_PAIRED_AB_R2

TASK_CHARACTER:
    SECOND_SEPARATELY_AUTHORIZED_CONTINUATION_OF_THE_SAME_EXACT_R1E_ROUTED_DESIGN

PRIMARY_PURPOSE:
    Complete the already-routed R1e experiment without rebuilding and without
    changing FPGA source.

    The exact routed DCP is already a valid scientific implementation object.

    Two earlier execution boundaries occurred after that implementation was
    created:

        1. the original build stopped before `write_bitstream` because a
           report-only `report_property` call received two objects;

        2. the first post-route continuation stopped even earlier because its
           task-local harness compared a checkpoint design-object name with the
           RTL top identifier.

    Neither boundary changed the source, routed DCP, hardware or scientific
    experiment.

R2_CORRECTIONS:

    namespace-correct design identity:
        the checkpoint current-design name and the RTL top identifier are
        recorded separately and are never required to be equal;

    exact DCP identity:
        SHA-256 plus frozen source/build manifest is the primary identity gate;

    bounded read-only dry run:
        one preflight Vivado session must execute the complete corrected
        identity/report harness without `write_bitstream`;

    bounded write continuation:
        one later session may apply only the original intended bitstream
        properties and execute exactly one `write_bitstream`;

    report-property correction:
        deterministic one-object-at-a-time reporting for both matched objects.

FULL_REBUILD_THIS_TASK:
    NO

FPGA_SOURCE_CHANGE_THIS_TASK:
    NO

NEW_SOURCE_COMMIT_THIS_TASK:
    NO

READ_ONLY_DCP_PREFLIGHT_SESSIONS_MAX:
    1

WRITE_CAPABLE_CONTINUATION_SESSIONS_MAX:
    1

WRITE_BITSTREAM_ATTEMPTS_MAX:
    1

FPGA_PROGRAM_INVOCATIONS_MAX:
    2

WARM_REBOOTS_MAX:
    2

PROGRAM_RETRIES:
    0

FINAL_REPORT_COUNT:
    1

FINAL_ACTIVE_IMAGE:
    exact formal Phase 2
```

===================================================================== -1. STANDING OWNER AUTHORIZATION — NO INTERACTIVE CONFIRMATIONS

```text
OWNER_STANDING_AUTHORIZATION=
    GRANTED

OWNER_INTERACTIVE_APPROVAL_REQUIRED=
    NO

CODEX_MUST_REQUEST_ADDITIONAL_CONFIRMATION=
    NO

EXPECTED_OWNER_INTERACTIONS=
    0
```

The owner grants advance approval for every action explicitly authorized by
this prompt.

Do not pause to ask the owner to approve:

```text
task-local file creation;
read-only source/evidence inspection;
one read-only DCP harness preflight;
one corrected post-route write-capable continuation;
one `write_bitstream`;
all report-only Vivado commands;
one diagnostic FPGA program;
one exact formal FPGA program;
two authorized warm reboots;
two authorized post-reboot pinned-driver loads;
all authorized read-only BAR/telemetry accesses;
evidence sealing;
one normal evidence commit/push and remote verification.
```

A passing gate means continue automatically.

A failing or blocked gate means:

```text
preserve evidence;
write the single final report;
publish when safely possible;
hard-stop without asking whether to continue.
```

This pre-authorization does not permit any action outside the explicit scope or
above the numerical limits.

===================================================================== 0. AUTHORITATIVE R1e IMPLEMENTATION AND CONTINUATION HISTORY

Exact R1e source:

```text
SOURCE_COMMIT=
    f3d9e5cdcacfb6fdebed5e5fe8b9143ef226aebd

SOURCE_TREE=
    db8b5581a237e19905fd01c6d453793047bc3ba7

TRACKED_SOURCE_FILES_CHANGED=
    18
```

Validated scientific/source gates:

```text
PROTECTED_NVP_BLOBS_UNCHANGED=
    YES

PRE_INIT_DONE_EQUIVALENCE=
    PASS

PRE_INIT_DONE_EQUIVALENCE_CYCLES=
    1814611

ORDERED_NACK_LEGACY_WINDOW_AUDIT=
    PASS

PROBE_TARGET_COUNT=
    10000

PROBE_SIMULATION=
    PASS
```

Validated implementation:

```text
SYNTHESIS=
    PASS

PLACE=
    PASS

ROUTE=
    PASS

ROUTE_ERRORS=
    0

WNS_NS=
    +0.617

WHS_NS=
    +0.021

DRC_ERRORS=
    0

DRC_CRITICAL_WARNINGS=
    0

REQP_1839_COUNT=
    4

REQP_1839_ACCEPTED_BASELINE_COUNT=
    4
```

Exact checkpoint identities:

```text
SYNTH_DCP_SHA256=
    1B86629EF73506533022488C28F41B56027E9FA1D6CFCB855381B79D6F78C001

ROUTED_DCP_SHA256=
    1CA3F857F2E3655FD06E8BF283B6BEC8826568402160EE004D7B8CD4D110C0B1
```

Original post-route build boundary:

```text
FAILURE_PHASE=
    AFTER_FULL_ROUTE_BEFORE_WRITE_BITSTREAM

ERROR=
    [Common 17-56] 'report_property' expects exactly one object got '2'

REPORT_PROPERTY_MATCH_COUNT=
    2

BITSTREAM_ATTEMPTS=
    0
```

First separately authorized continuation:

```text
EVIDENCE_COMMIT=
    6f3241601d83862802e11a2049668c8430cd29d7

EVIDENCE_PACKAGE_SHA256=
    B2C20EBD965FF2DEEF95B203607C46A7060D3DAAEB7ECD2A97100017679BC2C4

CONTINUATION_SCRIPT_SHA256=
    F582DB9CE4CAE08EE34AC396D3199BBDCD9BAEB9FBF54F4AEDE38074FEA963BB

CONTINUATION_RESULT=
    BLOCKED_SINGLE_POST_ROUTE_CONTINUATION_TOP_IDENTITY_HARNESS_MISMATCH

OBSERVED_CHECKPOINT_CURRENT_DESIGN_NAME=
    checkpoint_PHASE3_routed

RTL_TOP_IDENTIFIER_FROM_BUILD_MANIFEST=
    ahd_capture_top_xdma

INVALID_ASSERTION=
    CHECKPOINT_CURRENT_DESIGN_NAME_EQUALS_RTL_TOP_IDENTIFIER

REPORT_PROPERTY_FIX_REACHED=
    NO

WRITE_BITSTREAM_ATTEMPTS=
    0

HARDWARE_ACTIONS=
    0
```

Correct interpretation:

```text
CHECKPOINT_CURRENT_DESIGN_NAME_NAMESPACE=
    VIVADO_IN_MEMORY_DESIGN_OBJECT_NAME

RTL_TOP_IDENTIFIER_NAMESPACE=
    HDL_ELABORATION_TOP_NAME

NAMES_REQUIRED_TO_MATCH=
    NO

PRIOR_MISMATCH_INDICATES_WRONG_DCP=
    NO

ROUTED_DCP_REMAINS_REUSABLE=
    YES_AFTER_EXACT_HASH_AND_STATE_REVALIDATION
```

Current scientific status:

```text
R1E_BITSTREAM=
    NOT_YET_PRODUCED

R1E_HARDWARE_SAMPLE=
    NOT_YET_RUN

R1E_SCIENTIFIC_RESULT=
    NOT_EVALUATED
```

This prompt is a new, separately authorized continuation.

It does not retroactively change either prior hard-stop record.

=====================================================================

1. REPORTING POLICY AND SINGLE AUTHORITATIVE FINAL REPORT
=====================================================================

Create exactly one authoritative scientific report for the completed R1e
experiment:

```text
V41_NVP_R1E_EXTENDED_OBSERVABILITY_FINAL_REPORT.md
```

It must integrate:

```text
exact source identity;
all original source/simulation/equivalence/build gates;
exact routed-DCP identity;
both post-route harness boundaries;
namespace-correct identity method;
corrected report_property handling;
the generated bit identity;
Arm A;
full Arm B;
ordered NACK/lifecycle/probe analysis;
final formal Phase-2 state;
complete operation accounting.
```

Preserve all earlier hard-stop reports and raw logs unchanged under:

```text
00_CONTINUATION_INPUT/
```

They remain historical execution artifacts.

Do not delete, rewrite or overwrite them.

The authoritative final report may summarize the two host-harness boundaries
in one concise methods subsection:

```text
the implementation reached a fully routed DCP;
the first build-tail execution stopped on report-property cardinality;
the first continuation stopped on a namespace-invalid design-name assertion;
the R2 continuation reused the exact same DCP and corrected only task-local
harness logic.
```

Do not present either harness defect as a scientific NVP result.

────────

1.1 Pre-task JTAG reporting rule

The temporary pre-task JTAG handling incident was outside the executed
scientific campaign and did not affect the source, DCP, bitstream or sample.

Do not discuss that incident in:

```text
the final scientific report;
executive summary;
scientific classification;
publication receipt.
```

Do not alter raw evidence.

Record only the fresh task observation:

```text
JTAG_PRECHECK=
    PASS / FAIL
```

===================================================================== 2. EXACT SOURCE, BUILD, DCP, AND HARDWARE IDENTITIES

Formal repository:

```text
GITHUB_REPOSITORY=
    lukaszsudul/FPGA_AHD
```

R1e source branch:

```text
BRANCH=
    diag/v41-nvp-i2c-25khz-r1e-observability
```

Exact source:

```text
SOURCE_COMMIT=
    f3d9e5cdcacfb6fdebed5e5fe8b9143ef226aebd

SOURCE_TREE=
    db8b5581a237e19905fd01c6d453793047bc3ba7
```

Exact implementation checkpoint:

```text
ROUTED_DCP_SHA256=
    1CA3F857F2E3655FD06E8BF283B6BEC8826568402160EE004D7B8CD4D110C0B1

SYNTH_DCP_SHA256=
    1B86629EF73506533022488C28F41B56027E9FA1D6CFCB855381B79D6F78C001

EXPECTED_PART=
    xc7a35tcsg325-2

RTL_TOP_FROM_BUILD_MANIFEST=
    ahd_capture_top_xdma

PREVIOUSLY_OBSERVED_CHECKPOINT_CURRENT_DESIGN_NAME=
    checkpoint_PHASE3_routed

DESIGN_NAME_EQUALITY_REQUIRED=
    NO

PRIMARY_DCP_IDENTITY=
    ROUTED_DCP_SHA256_PLUS_FROZEN_SOURCE_BUILD_MANIFEST
```

R1e profile:

```text
AUTOINIT_I2C_HZ=
    25000

CLK_HZ=
    62500000

TICK_CYCLES=
    1251

EXPECTED_CNT_AT_INIT_DONE=
    132584734

PROBE_ADDRESS_BYTE=
    0x60

PROBE_TARGET_COUNT=
    10000
```

Protected R1 observer blobs:

```text
rtl/v41/axi_clock_lifecycle_monitor.sv
    c8c8145b12494cbdd54de7760f0558e6ab5fef11

rtl/v41/axi_clock_measurement_regs.sv
    64f4a3df745bcde00f9facac3030637a32a19485
```

Protected NVP blobs remain those recorded by the original R1e input manifest.

Exact formal Phase-2 control:

```text
FORMAL_CHECKPOINT_COMMIT=
    c89e88bcdf389614c884fb129e8b2d42a585bccb

FORMAL_CHECKPOINT_TREE=
    417820c69c134161fcafae0947dc5976919814d1

FORMAL_BIT_SHA256=
    7E4E909D78C2BE5C1E0ECA56229F2B88ECC2DBF5974B32BDF07EF1AFCD9449A2

FORMAL_RUNTIME_IDENTITY=
    A40A0C07 / 0000400B / 00031002

FORMAL_DIAGNOSTIC_MAGIC=
    0x00000000
```

FPGA/JTAG:

```text
HS2_SERIAL=
    210241768436

PART=
    xc7a35t

IDCODE=
    0362D093
```

Ubuntu:

```text
DUT=
    vcdeagent1@10.132.1.111

REQUIRED_KERNEL=
    7.0.0-29-generic
```

Pinned XDMA module:

```text
PATH=
    /home/vcdeagent1/FPGA_AHD_HOST/dma_ip_drivers/XDMA/linux-kernel/xdma/xdma.ko

SHA256=
    1AEF06244DF308FEE544A2662B73855C81BEB88BC929E7885D8D113E474B320A

VERMAGIC_PREFIX=
    7.0.0-29-generic
```

Accepted loader:

```text
PATH=
    /home/vcdeagent1/FPGA_AHD_HOST/phase2_precheck_accepted/phase2_load_xdma_driver.sh

SHA256=
    7279E316A2D647826B8D5EC6BEDF78A143B72D100B4008C7AC006C1B6653649F

INVOCATION=
    sudo /usr/bin/bash <loader> <exact-module> <module-sha> <fresh-dir>
```

Use the corrected Python BAR parser and accepted vendor-startup-HIGH plus
same-session-DONE programming observer from R1c/R1d.

===================================================================== 3. AUTHORIZATION AND LIMITS

Authorized offline:

```text
read and hash all prior R1e evidence;
create one new continuation task root;
create task-local namespace-correct Tcl/PowerShell harnesses;
run static fixtures;
open the exact routed DCP in one read-only preflight session;
execute all corrected report-only commands in that session;
open the same exact routed DCP in one write-capable continuation session;
apply only original intended BITSTREAM.* properties;
execute exactly one write_bitstream;
hash and package the generated bit;
run host-tool fixtures;
publish one consolidated evidence package.
```

Authorized hardware:

```text
Arm A:
    one R1e bit program;
    one frozen modeled wait;
    one warm reboot;
    at most one exact pinned-driver load;
    full R1e read-only telemetry;
    one final read-only DONE.

Arm B:
    one exact formal Phase-2 program;
    one normal wait;
    one warm reboot;
    at most one exact pinned-driver load;
    full formal read-only control telemetry;
    one final read-only DONE.
```

Maximum:

```text
FULL_BUILDS_THIS_TASK=
    0

SYNTHESIS_RUNS_THIS_TASK=
    0

PLACE_RUNS_THIS_TASK=
    0

ROUTE_RUNS_THIS_TASK=
    0

READ_ONLY_DCP_PREFLIGHT_SESSIONS=
    1

WRITE_CAPABLE_CONTINUATION_SESSIONS=
    1

WRITE_BITSTREAM_ATTEMPTS=
    1

FPGA_PROGRAM_INVOCATIONS=
    2

WARM_REBOOTS=
    2

POST_REBOOT_DRIVER_LOADS=
    2

PROGRAM_RETRIES=
    0
```

Not authorized:

```text
source edit;
new FPGA source commit;
new synthesis;
new opt_design;
new place_design;
new phys_opt_design;
new route_design;
incremental implementation;
write_checkpoint;
replacement routed DCP;
second read-only preflight;
second write-capable continuation;
second bitstream attempt;
second R1e hardware sample;
formal bootstrap program;
cold start;
power cycle;
physical action;
PCIe remove/rescan/reset;
AXI-Lite write;
NVP register/data write outside the already-built probe;
DMA;
Phase 3;
XDMA development;
tag;
Release.
```

===================================================================== 4. TASK ROOT

Create:

```text
C:\FPGA\V41_NVP_R1E_POST_ROUTE_CONTINUATION_R2\
```

Required structure:

```text
00_CONTINUATION_INPUT\
01_IDENTITY\
02_NAMESPACE_GATE\
03_REPORT_PROPERTY_AUDIT\
04_BUILD_TAIL_RECONCILIATION\
05_READ_ONLY_DRY_RUN\
06_WRITE_CONTINUATION\
07_BITSTREAM\
08_HOST_TOOL_PREFLIGHT\
09_HARDWARE_PRECHECK\
10_ARM_A_R1E\
11_ARM_B_FORMAL\
12_ANALYSIS\
13_FINAL\
scripts\
fixtures\
```

Create:

```text
OPERATION_LEDGER.md
TOOL_COMMAND_LEDGER.md
TIME_LEDGER.md
```

Initial accounting:

```text
FULL_BUILDS_THIS_TASK=0
SYNTHESIS_RUNS_THIS_TASK=0
PLACE_RUNS_THIS_TASK=0
ROUTE_RUNS_THIS_TASK=0
READ_ONLY_DCP_PREFLIGHT_SESSIONS=0
WRITE_CAPABLE_CONTINUATION_SESSIONS=0
WRITE_BITSTREAM_ATTEMPTS=0
FPGA_PROGRAMS=0
WARM_REBOOTS=0
DRIVER_LOADS=0
PROGRAM_RETRIES=0
SOURCE_CHANGES=0
FORMAL_REPOSITORY_MUTATIONS=0
```

Save this prompt verbatim and hash it before any Vivado command.

===================================================================== 5. P0 — EXACT ROUTED-DCP IDENTITY AND NAMESPACE-CORRECT DESIGN GATE

Locate only the exact prior R1e task roots and evidence:

```text
the original fully routed R1e build root;
the first continuation root;
evidence commit 6f3241601d83862802e11a2049668c8430cd29d7.
```

Do not use a similarly named checkpoint.

Preserve:

```text
prior reports;
source/build manifests;
operation ledgers;
build and continuation scripts;
Vivado logs/journals;
simulation/equivalence reports;
address-map audit;
probe simulation;
synth DCP;
routed DCP;
all pre-failure reports.
```

Require exact hashes:

```text
ROUTED_DCP_SHA256=
    1CA3F857F2E3655FD06E8BF283B6BEC8826568402160EE004D7B8CD4D110C0B1

SYNTH_DCP_SHA256=
    1B86629EF73506533022488C28F41B56027E9FA1D6CFCB855381B79D6F78C001

SOURCE_COMMIT=
    f3d9e5cdcacfb6fdebed5e5fe8b9143ef226aebd

SOURCE_TREE=
    db8b5581a237e19905fd01c6d453793047bc3ba7
```

────────

5.1 Namespace-correct identity semantics

The following are different namespaces:

```text
CHECKPOINT_CURRENT_DESIGN_NAME:
    a Vivado in-memory design/checkpoint object name;

RTL_TOP_FROM_BUILD_MANIFEST:
    the HDL elaboration top identifier.
```

Required:

```text
DESIGN_NAME_EQUALITY_REQUIRED=
    NO
```

The harness must record both fields independently.

Never execute a gate equivalent to:

```tcl
if {[current_design] ne "ahd_capture_top_xdma"} { error ... }
```

Never compare:

```text
checkpoint_PHASE3_routed
```

with:

```text
ahd_capture_top_xdma
```

as an equality requirement.

Recommended task-local Tcl behavior:

```tcl
set d [current_design]
set checkpoint_design_name $d
set checkpoint_part [get_property PART $d]

puts "CHECKPOINT_CURRENT_DESIGN_NAME=$checkpoint_design_name"
puts "RTL_TOP_FROM_BUILD_MANIFEST=ahd_capture_top_xdma"
puts "DESIGN_NAME_EQUALITY_REQUIRED=NO"

if {$checkpoint_design_name eq ""} {
    error "no current design is open"
}

if {$checkpoint_part ne "xc7a35tcsg325-2"} {
    error "unexpected part: $checkpoint_part"
}
```

The exact DCP hash is the primary checkpoint identity.

────────

5.2 Required identity gates

After opening the exact DCP, require:

```text
DCP_SHA256=exact;
source commit/tree/build-manifest chain=exact;
CURRENT_DESIGN_NAME=nonempty informational value;
PART=xc7a35tcsg325-2;
IS_ROUTED=YES;
ROUTE_ERRORS=0;
expected top-level port signature present;
expected R1e structural capability signature present;
timing/DRC/CDC/REQP-1839 compatible with frozen reports.
```

The expected port and structural signatures must be derived from:

```text
the exact source manifest;
the exact prior DCP inventory;
the exact original build reports.
```

Do not invent a new brittle hierarchy-name requirement.

A structural name optimized by Vivado may be located by connectivity or
ORIG_REF_NAME.

Do not use the structural signature as a replacement for the exact DCP hash.

────────

5.3 Namespace-gate fixtures

Before opening the DCP, run deterministic fixtures:

```text
FIXTURE_1:
    DCP hash exact
    checkpoint name checkpoint_PHASE3_routed
    RTL top ahd_capture_top_xdma
    part exact
    routed yes
    expected result PASS

FIXTURE_2:
    DCP hash wrong
    all names plausible
    expected result FAIL

FIXTURE_3:
    DCP hash exact
    current design empty
    expected result FAIL

FIXTURE_4:
    DCP hash exact
    part wrong
    expected result FAIL
```

Static scan the continuation scripts.

Require:

```text
CURRENT_DESIGN_TO_RTL_TOP_EQUALITY_COMPARISON_COUNT=
    0

ROUTED_DCP_HASH_GATE_PRESENT=
    YES

PART_GATE_PRESENT=
    YES

ROUTED_STATE_GATE_PRESENT=
    YES
```

Create:

```text
02_NAMESPACE_GATE\
    DESIGN_IDENTITY_SEMANTICS.md

02_NAMESPACE_GATE\
    DESIGN_IDENTITY_FIXTURE_RESULTS.csv

02_NAMESPACE_GATE\
    CONTINUATION_SCRIPT_NAMESPACE_STATIC_AUDIT.md
```

Any identity contradiction:

```text
BLOCKED_PRIOR_ROUTED_DCP_IDENTITY_OR_STATE
```

No rebuild.

===================================================================== 6. P1 — EXACT report_property FAILURE AUDIT

Locate the original [Common 17-56] failure in:

```text
the original build Vivado log;
the exact original build Tcl script.
```

The first continuation did not reach this command.

Record:

```text
FAILED_TCL_LINE=
FAILED_OBJECT_QUERY=
FAILED_OBJECT_COUNT=2
FAILED_REPORT_PURPOSE=
FIRST_CONTINUATION_REACHED_THIS_COMMAND=
    NO
```

Evaluate the exact object query against the exact routed DCP.

Do not use:

```tcl
lindex $objects 0
```

as a silent fix.

────────

6.1 Multi-object report semantics

If the report was intended to document every matched object:

```text
REPORT_PROPERTY_CARDINALITY_FIX=
    DETERMINISTIC_FOREACH_ALL_MATCHED_OBJECTS
```

Requirements:

```text
sort objects deterministically by full hierarchical name;
require exact expected count=2;
emit one object header per object;
call report_property with exactly one object per invocation;
preserve all properties for both objects;
produce one combined index and one file per object.
```

────────

6.2 Single-object gate semantics

If the original gate logically required exactly one object:

```text
REPORT_PROPERTY_CARDINALITY_FIX=
    NOT_PERMITTED_DUPLICATE_OBJECT_CONTRADICTION

CLASSIFICATION=
    BLOCKED_REPORT_GATE_EXPECTED_ONE_OBJECT_FOUND_TWO
```

Hard stop.

Do not choose one arbitrarily.

────────

6.3 Static report-command audit

Before any DCP session require:

```text
every report_property invocation receives exactly one object;
expected query count is explicit;
no source-edit command;
no synthesis/place/route command;
no write_checkpoint;
no implementation-changing property;
write_bitstream absent from dry-run mode;
exactly one possible write_bitstream in write mode;
no retry loop.
```

Create:

```text
03_REPORT_PROPERTY_AUDIT\
    FAILED_COMMAND_ROOT_CAUSE.md

03_REPORT_PROPERTY_AUDIT\
    OBJECT_CARDINALITY.csv

03_REPORT_PROPERTY_AUDIT\
    REPORT_PROPERTY_FIXTURE_RESULTS.csv

03_REPORT_PROPERTY_AUDIT\
    CONTINUATION_SCRIPT_STATIC_AUDIT.md
```

===================================================================== 7. P2 — ORIGINAL BUILD-TAIL RECONCILIATION

Audit the original build script from the first post-route report command through
its end.

Create an ordered command inventory:

```text
original line;
command;
purpose;
already completed before the original failure;
validated in read-only dry run;
required in write continuation;
forbidden in continuation.
```

Allowed across the two R2 sessions:

```text
read-only session:
    namespace-correct identity gates;
    corrected report_property commands;
    every remaining report-only command;

write session:
    namespace-correct identity gates;
    exact original intended BITSTREAM.* properties;
    one write_bitstream;
    post-write file/hash checks.
```

Forbidden:

```text
any design optimization;
any placement/routing;
any new constraint;
any implementation-changing property;
any property absent from the original intended bitstream tail, except
task-local output paths and report formatting.
```

Require:

```text
ORIGINAL_BUILD_TAIL_RECONCILED=
    PASS

ORIGINAL_REPORT_ONLY_TAIL_ASSIGNED_TO_DRY_RUN=
    YES

ORIGINAL_BITSTREAM_TAIL_ASSIGNED_TO_WRITE_SESSION=
    YES

CONTINUATION_FUNCTIONAL_SEMANTICS_CHANGE=
    NO
```

Create:

```text
04_BUILD_TAIL_RECONCILIATION\
    ORIGINAL_BUILD_TAIL_COMMAND_MATRIX.csv

04_BUILD_TAIL_RECONCILIATION\
    BUILD_TAIL_RECONCILIATION.md
```

===================================================================== 8. P3 — READ-ONLY DRY RUN, THEN ONE WRITE-CAPABLE CONTINUATION

Use one shared, statically audited Tcl helper library for:

```text
DCP hash/provenance input checks performed by the Windows supervisor;
namespace-correct in-DCP identity;
part/routed-state gates;
deterministic object sorting;
per-object report_property;
report generation;
event/ledger output.
```

The write-capable script must call the same helper procedures.

────────

8.1 Session A — one read-only DCP harness preflight

Create:

```text
scripts\preflight_exact_r1e_routed_dcp_r2.tcl
```

It must:

```text
open the exact frozen routed DCP;
record current-design name without comparing it to the RTL top;
verify exact part and routed state;
run the complete corrected two-object report_property sequence;
run every remaining report-only command from the reconciled original tail;
write all reports to 05_READ_ONLY_DRY_RUN;
perform no BITSTREAM property assignment;
contain no write_bitstream;
close without saving.
```

Run exactly once.

Require:

```text
READ_ONLY_DCP_PREFLIGHT_SESSIONS=
    1

PROCESS_EXIT_CODE=
    0

DESIGN_IDENTITY_GATE=
    PASS_NAMESPACE_CORRECT

CHECKPOINT_CURRENT_DESIGN_NAME=
    recorded

RTL_TOP_FROM_BUILD_MANIFEST=
    ahd_capture_top_xdma

DESIGN_NAME_EQUALITY_REQUIRED=
    NO

REPORT_PROPERTY_OBJECT_COUNT=
    2

REPORT_PROPERTY_PER_OBJECT_OUTPUTS=
    2

ALL_REMAINING_REPORT_ONLY_COMMANDS=
    PASS

DCP_SAVED_OR_MODIFIED=
    NO

WRITE_BITSTREAM_COMMAND_COUNT=
    0
```

If the dry run fails:

```text
no write-capable continuation;
no bitstream;
seal evidence and hard-stop.
```

────────

8.2 Session B — one write-capable continuation

Proceed only after Session A PASS.

Create:

```text
scripts\continue_exact_r1e_from_routed_dcp_r2.tcl
```

It must:

```text
open the exact same frozen routed DCP;
repeat the namespace-correct identity, part and routed-state gates;
verify the exact DCP file hash through the supervisor before launch;
apply only original intended BITSTREAM.* properties;
write exactly one bitstream;
perform post-write existence/size checks;
exit nonzero on any error.
```

Do not rerun report-only commands unnecessarily in this session; their exact
harness was already proven in Session A.

Bit filename:

```text
ahd_capture_v41_i2c_25khz_r1e_observability.bit
```

Output:

```text
07_BITSTREAM\
    ahd_capture_v41_i2c_25khz_r1e_observability.bit
```

Run exactly once through the supported Vivado launcher.

Require:

```text
WRITE_CAPABLE_CONTINUATION_SESSIONS=
    1

WRITE_BITSTREAM_ATTEMPTS=
    1

PROCESS_EXIT_CODE=
    0

DESIGN_IDENTITY_GATE=
    PASS_NAMESPACE_CORRECT

BITSTREAM_EXISTS=
    YES

BITSTREAM_SIZE_NONZERO=
    YES

SOURCE_OR_DCP_MUTATED=
    NO
```

Compute:

```text
R1E_BIT_SHA256=
```

Preserve exact stdout/stderr/log/journal for both sessions.

No second preflight, continuation or bitstream attempt.

===================================================================== 9. P4 — POST-BITSTREAM OFFLINE GATES

Require:

```text
R1E_BIT_SOURCE_COMMIT=
    f3d9e5cdcacfb6fdebed5e5fe8b9143ef226aebd

R1E_BIT_SOURCE_TREE=
    db8b5581a237e19905fd01c6d453793047bc3ba7

R1E_BUILD_FLAGS_EXPECTED=
    0x00000002

FULL_BUILDS_TOTAL_EXPERIMENT=
    1

FULL_BUILDS_THIS_TASK=
    0

ROUTED_DCP_REUSED=
    YES_EXACT_SHA256_1CA3F857F2E3655FD06E8BF283B6BEC8826568402160EE004D7B8CD4D110C0B1

SOURCE_CHANGES_THIS_TASK=
    0
```

Reconcile all required R1e capabilities against the routed DCP and source
manifest:

```text
lifecycle monitor present;
expected constants present;
probe target count=10000;
probe address byte=0x60;
probe I2C Hz=25000;
existing legacy NACK window present;
R1e page present;
pre-init equivalence evidence PASS;
probe simulation PASS.
```

Create a sealed build-completion package containing:

```text
source identity;
prior synth/routed DCP identity;
continuation script;
corrected property reports;
all post-route reports;
bit;
bit SHA;
Vivado logs;
continuation ledger.
```

Do not call this a second build.

===================================================================== 10. P5 — HOST-TOOL AND WAIT PREFLIGHT

Reuse the exact R1e host decoders from the prior task when their hashes match.

Require fixture coverage for:

```text
lifecycle page decoding;
coherent 48-bit counter read;
R1e capability/probe page;
legacy header 0x10098;
16 log words 0x1009C..0x100D8;
record decode;
overflow;
NACK_COUNT<=8 consistency;
NACK_COUNT>8 overflow consistency;
Wilson interval;
zero-NACK upper bound;
probe-count invariants.
```

Freeze the Arm-A wait.

Use the exact prior simulation/model result:

```text
MODELED_PROBE_COMPLETE_SECONDS_FROM_CONFIGURATION=
```

Require that value from the prior R1e simulation evidence.

Then:

```text
ARM_A_REQUIRED_WAIT_SECONDS =
    max(
        10.000000,
        MODELED_PROBE_COMPLETE_SECONDS_FROM_CONFIGURATION + 2.000000
    )
```

Record the exact frozen value before hardware.

If the prior modeled value is absent, derive it from the exact source and
previously passed simulation artifacts without changing source, then require
model/log agreement.

No hardware if the wait cannot be proven.

===================================================================== 11. P6 — FRESH HARDWARE PRECHECK

The owner reports that the physical JTAG connection is restored and tested.

Treat that as context only.

Fresh task evidence is authoritative.

Require read-only:

```text
one HS2 210241768436 target;
one xc7a35t;
IDCODE 0362D093;
fresh DONE=1;
current kernel 7.0.0-29-generic;
next warm reboot proven to remain kernel 29;
one endpoint 10ee:7011 / 10ee:0007;
Gen1 x1;
BAR0 128 KiB;
BAR1 64 KiB;
corrected Python BAR-parser PASS;
formal identity A40A0C07 / 0000400B / 00031002;
diagnostic magic=0;
exact pinned driver, or one permitted pre-Arm-A exact load;
no wrong same-name xdma;
no open XDMA node;
zero DMA;
kernel/AER health PASS;
no stale Vivado/hw_server owner.
```

No bootstrap is authorized.

If exact formal Phase 2 cannot be proven:

```text
BLOCKED_REQUIRED_FORMAL_START_STATE
```

with zero programs.

===================================================================== 12. ARM A — R1e EXTENDED-OBSERVABILITY IMAGE

Immediately before program:

```text
rehash exact R1e bit;
verify bit path;
verify source/DCP/bit manifest;
verify target;
verify operation ledger.
```

Program once using the accepted observer.

Require:

```text
vendor startup HIGH;
same-session DONE=1;
one invocation;
exit code 0;
no retry.
```

Wait exactly at least the frozen:

```text
ARM_A_REQUIRED_WAIT_SECONDS
```

using the common monotonic QPC method.

Perform one warm reboot.

Require:

```text
new boot ID;
kernel 7.0.0-29-generic;
corrected BAR geometry;
exact explicit-path XDMA load;
expected node set;
host/kernel/AER health.
```

Verify runtime:

```text
GIT_SHA=
    f3d9e5cdcacfb6fdebed5e5fe8b9143ef226aebd

BUILD_FLAGS=
    0x00000002

common identity=
    A40A0C07 / 0000400B / 00031002

R1 lifecycle magic/version=
    PASS

R1e magic/version/capabilities=
    PASS

ACTIVE_I2C_HZ=
    25000

PROBE_TARGET_COUNT=
    10000
```

Collect two full read-only snapshots approximately one second apart.

Each snapshot includes:

```text
normal NVP/video fields;

0x2000..0x2094 lifecycle/R1e page;

0x10098..0x100D8 legacy NACK header/log window.
```

Static coherence required for:

```text
CNT_AT_INIT_DONE;
expected constants;
probe counters/status;
ordered-log header/records;
INIT_DONE/ERROR;
NACK/TIMEOUT;
FIRST_ERROR;
original/restored FF.
```

Valid instrumentation sample:

```text
PROBE_DONE=1
PROBE_ABORTED=0
PROBE_COUNT=10000
PROBE_ACK_COUNT+PROBE_NACK_COUNT=10000
PROBE_TIMEOUT_COUNT=0
lifecycle counter coherent
ordered-log consistency PASS
final DONE=1
```

Classify NVP PASS/FAIL separately.

No second Arm-A run.

===================================================================== 13. ARM B — FULL EXACT FORMAL CONTROL

Program exact formal Phase 2 once.

Require the same programming gates and no retry.

Wait at least 5 seconds.

Perform one warm reboot.

Require:

```text
kernel 29;
corrected BARs;
exact pinned driver;
formal identity;
diagnostic magic=0;
host/kernel health.
```

Collect the same normal NVP/video T0/T1 fields.

Additionally read:

```text
0x10098..0x100D8:
    formal legacy NACK header/log window

0x2000..0x20FF:
    expected deterministic zero/reserved page
```

Require:

```text
full Arm-B T0/T1 functional control;
ordered-log consistency;
R1e page zero;
final DONE=1.
```

At task end:

```text
FINAL_ACTIVE_IMAGE=FORMAL_PHASE2
FINAL_PINNED_DRIVER_LOADED=YES
FINAL_DONE=1
```

Arm B is not restoration-only.

===================================================================== 14. SCIENTIFIC ANALYSIS

Produce:

```text
Arm-A lifecycle shortening/extension;
Arm-A ordered first-eight NACK records and overflow;
Arm-A 10,000-probe NACK rate and confidence interval;
Arm-A NVP functional result;

Arm-B aggregate NACK;
Arm-B ordered first-eight records and overflow;
Arm-B NVP functional result;

paired comparison;
control-flow/log reconciliation;
probe-versus-autoinit interpretation.
```

Required limitations:

```text
ordered log capacity=8;
overflow means first eight only;
probe measures post-autoinit write-address ACK only;
probe is active but non-register-writing;
new logic may alter implementation;
analog voltage/rise time is not directly measured.
```

Always state:

```text
ROOT_CAUSE_SOLELY_PROVEN=NO
BOARD_VCCO_DROOP_PROVEN=NO
GROUND_BOUNCE_PROVEN=NO
ANALOG_MARGIN_DIRECTLY_MEASURED=NO
```

===================================================================== 15. EVIDENCE AND PUBLICATION

Evidence repository:

```text
lukaszsudul/AHD-diagnostic-evidence
```

Preferred path:

```text
v41-nvp-r1e-extended-observability-r1/
```

If that path already contains the earlier incomplete-stage evidence:

```text
preserve all existing files and Git history;
add the continuation and final evidence in a normal new commit;
place the one authoritative final report at the path root;
do not force-push or rewrite history.
```

Required evidence:

```text
verbatim R2 continuation prompt;
prior continuation evidence commit 6f3241601d83862802e11a2049668c8430cd29d7;
prior continuation evidence ZIP SHA B2C20EBD965FF2DEEF95B203607C46A7060D3DAAEB7ECD2A97100017679BC2C4;
prior R1e source/build identities;
prior routed DCP hash;
failed-command audit;
object cardinality;
continuation script;
corrected per-object property reports;
post-route reports;
bit and hash;
host-tool fixtures;
hardware precheck;
raw Arm-A and Arm-B evidence;
decoded logs;
lifecycle/probe statistics;
single final report;
operation ledger;
secret scan;
SHA-256 manifest.
```

Create one sealed ZIP and sidecar.

No PDF or DOCX.

Normal commit/push only.

No tag or Release.

===================================================================== 16. REQUIRED SINGLE FINAL REPORT BLOCK

The one final report must end with:

```text
TASK=
    V41_NVP_R1E_NAMESPACE_CORRECT_POST_ROUTE_CONTINUATION_AND_PAIRED_AB_R2

PRIOR_CONTINUATION_EVIDENCE_COMMIT=
    6f3241601d83862802e11a2049668c8430cd29d7

PRIOR_CONTINUATION_EVIDENCE_PACKAGE_SHA256=
    B2C20EBD965FF2DEEF95B203607C46A7060D3DAAEB7ECD2A97100017679BC2C4

PRIOR_CONTINUATION_BLOCKER=
    BLOCKED_SINGLE_POST_ROUTE_CONTINUATION_TOP_IDENTITY_HARNESS_MISMATCH

FINAL_REPORT_COUNT=
    1

SOURCE_COMMIT=
    f3d9e5cdcacfb6fdebed5e5fe8b9143ef226aebd

SOURCE_TREE=
    db8b5581a237e19905fd01c6d453793047bc3ba7

TRACKED_SOURCE_FILES_CHANGED=
    18

PROTECTED_NVP_BLOBS_UNCHANGED=
    YES

PRE_INIT_DONE_EQUIVALENCE=
    PASS

PRE_INIT_DONE_EQUIVALENCE_CYCLES=
    1814611

ORDERED_NACK_LEGACY_WINDOW_AUDIT=
    PASS

PROBE_TARGET_COUNT=
    10000

PROBE_SIMULATION=
    PASS

PRIOR_SYNTHESIS=
    PASS

PRIOR_PLACE=
    PASS

PRIOR_ROUTE=
    PASS

PRIOR_ROUTE_ERRORS=
    0

PRIOR_WNS_NS=
    +0.617

PRIOR_WHS_NS=
    +0.021

PRIOR_DRC_ERRORS=
    0

PRIOR_DRC_CRITICAL_WARNINGS=
    0

PRIOR_REQP_1839_COUNT=
    4

PRIOR_ROUTED_DCP_SHA256=
    1CA3F857F2E3655FD06E8BF283B6BEC8826568402160EE004D7B8CD4D110C0B1

CHECKPOINT_CURRENT_DESIGN_NAME=
RTL_TOP_FROM_BUILD_MANIFEST=
    ahd_capture_top_xdma
DESIGN_NAME_EQUALITY_REQUIRED=
    NO
DESIGN_IDENTITY_PRIMARY_GATE=
    EXACT_DCP_SHA256_PLUS_SOURCE_BUILD_MANIFEST
DESIGN_IDENTITY_GATE=
    PASS_NAMESPACE_CORRECT

CURRENT_DESIGN_TO_RTL_TOP_EQUALITY_COMPARISON_COUNT=
    0

REPORT_PROPERTY_ROOT_CAUSE=
    MULTI_OBJECT_LIST_PASSED_TO_SINGLE_OBJECT_COMMAND

REPORT_PROPERTY_MATCH_COUNT=
    2

REPORT_PROPERTY_FIX=
    DETERMINISTIC_PER_OBJECT_REPORTING

SOURCE_CHANGES_THIS_TASK=
    0

FULL_BUILDS_THIS_TASK=
    0

SYNTHESIS_RUNS_THIS_TASK=
    0

PLACE_RUNS_THIS_TASK=
    0

ROUTE_RUNS_THIS_TASK=
    0

READ_ONLY_DCP_PREFLIGHT_SESSIONS=
    1

WRITE_CAPABLE_CONTINUATION_SESSIONS=
    1

WRITE_BITSTREAM_ATTEMPTS=
    1

R1E_BIT_SHA256=
R1E_BIT_SOURCE_COMMIT=
R1E_BIT_SOURCE_TREE=

ARM_A_REQUIRED_WAIT_SECONDS=
ARM_A_PROGRAM=
ARM_A_KERNEL=
ARM_A_DRIVER=
ARM_A_RUNTIME_PROVENANCE=
ARM_A_CNT_AT_INIT_DONE=
ARM_A_SIGNED_COUNT_ERROR_CYCLES=
ARM_A_SHORTENING_CYCLES=
ARM_A_SHORTENING_TICKS=
ARM_A_NACK_COUNT=
ARM_A_NACK_LOG_COUNT=
ARM_A_NACK_LOG_OVERFLOW=
ARM_A_ORDERED_NACK_RECORDS=
ARM_A_PROBE_COUNT=
ARM_A_PROBE_ACK_COUNT=
ARM_A_PROBE_NACK_COUNT=
ARM_A_PROBE_TIMEOUT_COUNT=
ARM_A_PROBE_NACK_RATE=
ARM_A_PROBE_WILSON95=
ARM_A_NVP_RESULT=
ARM_A_FINAL_DONE=

ARM_B_PROGRAM=
ARM_B_KERNEL=
ARM_B_DRIVER=
ARM_B_FORMAL_IDENTITY=
ARM_B_DIAGNOSTIC_MAGIC=
ARM_B_R1E_PAGE_ZERO=
ARM_B_NACK_COUNT=
ARM_B_NACK_LOG_COUNT=
ARM_B_NACK_LOG_OVERFLOW=
ARM_B_ORDERED_NACK_RECORDS=
ARM_B_NVP_RESULT=
ARM_B_FINAL_DONE=

PAIRED_AB_RESULT=
CONTROL_FLOW_SHORTENING_EXPLAINED_BY_LOG=
STOCHASTIC_ADDRESS_OR_BUS_MARGIN=
AUTOINIT_OPERATION_OR_PHASE_CONTEXT=
ROOT_CAUSE_SOLELY_PROVEN=
    NO

FINAL_ACTIVE_IMAGE=
    FORMAL_PHASE2

FINAL_PINNED_DRIVER_LOADED=
FINAL_DONE=

FPGA_PROGRAM_INVOCATIONS=
    2

WARM_REBOOTS=
    2

PROGRAM_RETRIES=
    0

COLD_STARTS=
    0

PHYSICAL_ACTIONS_DURING_TASK=
    0

AXI_LITE_WRITES=
    0

C2H_TRANSFERS=
    0

H2C_TRANSFERS=
    0

PHASE3_RESUMED=
    NO

XDMA_DEVELOPMENT_CONTINUED=
    NO

FORMAL_REPOSITORY_MUTATIONS=
    0

OWNER_INTERACTIVE_APPROVAL_REQUESTS=
    0

EVIDENCE_PACKAGE_SHA256=
EVIDENCE_REPOSITORY_COMMIT=
PUBLIC_REMOTE_VERIFICATION=
NEXT_ACTION=
    OWNER_AND_AUDITOR_REVIEW_OF_COMPLETED_R1E_RESULTS
```

===================================================================== 17. HARD STOPS

Hard-stop before the read-only dry run if:

```text
source/tree mismatch;
exact routed-DCP SHA mismatch;
namespace-gate fixtures fail;
a continuation script compares current-design name with RTL top;
report_property static audit fails;
original build tail cannot be reconciled.
```

Hard-stop after the dry run if:

```text
DCP does not open;
current design is empty;
part differs;
design is not routed;
route/timing/DRC/CDC materially contradict frozen evidence;
the exact report_property query does not return two objects;
per-object reports do not both complete;
any report-only tail command fails;
the dry-run script contains or invokes write_bitstream.
```

Hard-stop after the single write-capable continuation if:

```text
namespace-correct identity gate fails;
write_bitstream fails;
bit is absent/empty;
source/DCP/bit provenance cannot be proven.
```

Hard-stop before hardware if:

```text
fresh JTAG gate fails;
formal start state is not proven;
kernel/driver/BAR/endpoint gate fails;
host-tool fixtures fail;
modeled Arm-A wait is not frozen.
```

Hard-stop during hardware if:

```text
programming fails;
host does not return;
kernel changes;
driver cannot load once;
runtime provenance mismatch;
probe invariants fail;
ordered-log consistency fails;
lifecycle read is incoherent;
final DONE fails.
```

No:

```text
DCP replacement;
full rebuild;
source patch;
second dry run;
second write continuation;
second bitstream attempt;
program retry;
third program;
second Arm-A sample;
physical action;
Phase-3 work.
```

===================================================================== 18. BEGIN

```text
save and hash this prompt
    ->
ingest original R1e and prior-continuation evidence
    ->
freeze exact source/build/DCP identities
    ->
fixture-test namespace-correct identity semantics
    ->
audit the exact report_property two-object query
    ->
reconcile the original remaining build tail
    ->
Session A:
        open exact routed DCP read-only
        record current-design name separately from RTL top
        run exact identity/part/routed gates
        run deterministic per-object report_property
        run every remaining report-only command
        close without save or write_bitstream
    ->
Session B:
        open the same exact routed DCP
        repeat namespace-correct identity gates
        apply only original intended bitstream properties
        write exactly one R1e bit
    ->
hash and seal build-completion package
    ->
run host-tool and frozen-wait preflights
    ->
fresh JTAG/formal/kernel/driver/BAR precheck
    ->
Arm A R1e:
        program once
        frozen modeled wait
        reboot
        driver
        lifecycle + probe + ordered log + normal telemetry
        final DONE
    ->
Arm B formal:
        program once
        wait
        reboot
        driver
        full control telemetry + ordered log
        prove R1e page zero
        final DONE
    ->
combined analysis
    ->
leave exact formal Phase 2 active
    ->
create one authoritative final report
    ->
seal and publish evidence
    ->
HARD STOP.
```

No interactive owner questions.
No full rebuild.
No FPGA source changes.
No comparison of checkpoint current-design name with RTL top.
No discussion of the pre-task accidental JTAG disconnection in the final
scientific report.
No Phase 3.
No XDMA work.