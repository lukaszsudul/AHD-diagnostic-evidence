# W3a candidate 3: bounded static review before the one build

Source commit `70266f0b90c6fc6a853495eba1d526b285fd7286`, tree `5f2bd8377985406b45433418b20be8993399bcb6`, clean isolated worktree. The final diff against stable parent `09cd7cbb426027acaefd0cf3989579b80a451f3a` has 12 paths: five RTL paths and seven reduced host/contract paths. Capture, PCIe, DMA, ACQ, tables, active XDC, and SSOT were not edited. The full W3 telemetry child was replaced by a small retained bank-error/control register block; the inherited SCAN1 BRAM and 82-entry, 10-group scan stay in place.

The scanner enum has explicit five-bit states: idle 0; active states 1–9; host ACK 10; abort 11; hard error 12. The new W3a module receives the explicit five-bit state, first-raw bundle is five bits at the master, top, wrapper, scanner and W3a ports, and all new connected ports are declared before use. `git diff --check` passed. Synthesis/elaboration remains the connection-warning gate; this review is not a functional equivalence proof.

Candidate-3 Vivado elaboration logged the W3a module being synthesized and gave no W3a port-width, implicit-net, multiple-driver or truncation warning in user RTL. The connection warnings seen so far are in generated XDMA IP (`Synth 8-7071/8-7023`, and its unused pipeline ports). Four `Synth 8-6014` removed-register warnings in the scanner match named warnings in the candidate-2 log. The raw candidate-3 log remains authoritative for the complete synthesis warning audit.

The inherited candidate-2 pause control is unchanged: reset/default OFF, a block setting is latched at scan admission, and exactly 18,750 FPGA clocks gate the verify command after a successful group select or successful restore. An unsuccessful bank write follows the old failure/restore path without arming the pause. The timer does not enter the low-level I2C FSM or retry decisions. The 3,755-cycle OFF and 22,505-cycle ON observations belong to candidate-2 source `98d4d214c1219b29a60dc1a33af13e91fe33d27c`; the candidate-3 statement is `STATIC_REVIEW_ONLY`, not a new measurement.

The new bank record is latched on a failed bank transaction using the per-command first raw cause before the next command acceptance clears it. The first failed bank operation becomes the primary record. A later failed restore or restore verify becomes a separate cleanup record; neither legacy ACK nor a later clean scan clears them. A select-write failure records verify as `NOT_EXECUTED`; failed verify transports and value mismatches have separate values. The host checks W3AL magic, version, capabilities and exact contract hash before interpreting records, rejects unstable active-scan reads, and uses the existing explicit BDF/device mapping. Import/self-test opened no device.

New XSim/xvlog/xelab, alternative simulator, formal and full RTL regression runs: `NOT_RUN_OWNER_REQUESTED_SPEEDUP` (`XSIM_NEW_RUNS=0`). The old W3 T01–T32 matrix is not a W3a pass matrix. Parent SCAN1/capture results are inherited only for unchanged source, candidate-2 pause measurements and tests are historical for a different source, wide W3b record-array checks are outside the reduced scope, and changed W3a RTL behavior is not dynamically tested in this run. Behavioral assurance remains limited to static review, identified history and implementation checks.
