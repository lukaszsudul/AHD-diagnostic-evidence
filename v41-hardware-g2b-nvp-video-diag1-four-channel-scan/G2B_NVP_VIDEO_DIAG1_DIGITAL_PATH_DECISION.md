# Downstream digital-path decision

NOT_REACHED. Rotating BGDCOL capture was conditional on an offline-qualified diagnostic bitstream, which was not produced. The previous exact-black PRODUCT capture is not sufficient to decide whether non-black NVP VDO1 values traverse the FPGA path.
