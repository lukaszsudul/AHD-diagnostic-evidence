# Auto-detect fallback

`NOT_IMPLEMENTED_SAFELY`.

The PDF documents format classification in auto-detection mode, but this task's forced-AHD1080p25 PRODUCT baseline and reversible auto-detect transaction sequence were not proven as an exact, bounded runtime contract. This optional capability was therefore omitted.
