# AHD v41 CONT1R3R4R3 — Bank5 select failure replay (offline)

Engineering gate: **PASS** for this bounded offline investigation. Overall result: `PASS_OFFLINE_DATA_NACK_SIGNATURE_REPRODUCED`. The prior COLDSTART hardware campaign remains **FAIL**; its 1,000-scan endpoint was not reached. Evidence publication is evaluated separately after a non-force push and commit-pinned read-back; the future publication commit is intentionally not embedded in this payload.

The frozen scanner maps the fixed master's raw `CAUSE_DATA_NACK=0x4`, with timeout=0, to scanner `INTERNAL_PROTOCOL=0xA`. In the Level A registered mock replay, 26 clean generations completed and were acknowledged; the following injected Bank5 `0xFF=0x05` select DATA_NACK reproduced the entire retained SCAN1 hardware signature: status `0x00000C18`, generation 26 unchanged, 37 valid/0 failed/0 retried, first index 36, detail `0x0005FF0A`, flags `0x00330004`, 51 accepted commands, valid Bank0 entry/exit, no complete publication, and `0x00000011` after one ACK_CLEAR. The 26th attempted campaign scan was incomplete; generation 26 belonged to the preceding complete scan.

Level B exercised the **unchanged** scanner and fixed master at the actual 62.5 MHz/25 kHz profile with default SCL/idle timeout values 1250/62500 cycles. A task-owned resolved open-drain digital slave decoded emitted bytes `0x60,0xFF,0x05`, acknowledged address and register, and withheld only the data-byte ACK. The real master then generated raw cause 4 itself; SCAN1 produced detail `0x0005FF0A`, 37 valid entries, 51 commands and no generation advance. The short pin/byte/ACK timeline is in `simulation-receipts/T15_pin.log`. Three real-master negative controls produced raw 1/2/3 and scanner detail 1/2/3; verify-read RADDR NACK required 52 commands. Level B starts at simulated generation 0; Level A alone establishes the absolute historical generation 26.

All 18 named tests executed with assertion and completion-marker checks. T10 exercised the full 16×2 raw-cause/timeout table, separating ordinary master exits from synthetic interface combinations. T11 showed that synthetic failure with raw0/timeout0 can also map to `0xA`; a legal ordinary path to that combination was not shown. T03 proved that a final successful restore cannot distinguish whether Bank5 was internally accepted despite a missing ACK. Thus DATA_NACK is the only matching **ordinary master path within the inspected source and tests**, not a proven unique physical cause. The hardware receipt did not retain the failed command's raw master code or SCL/SDA pins; NVP non-acknowledgement versus timing/threshold/noise remains unproven. Changing error-label wording would not repair I2C communication.

The private 108145-byte archive passed exact SHA-256 and all 26 per-file manifest checks. The unchanged host schema independently re-decoded the control and 25 complete campaign records, all 11 successful-retry `REGADDR_NACK` events, 2,050 campaign exposures and 250 campaign group records. `campaign-0022` entry 19/group 3/position 8 remains included. The failed attempt was excluded from complete-record denominators. The preregistered causal result remains `NOT_ASSESSED_CAMPAIGN_INCOMPLETE`; read REGADDR_NACK and select-write DATA_NACK are not asserted to share an electrical cause.

No DUT, SSH, JTAG, real MMIO, driver, NVP, camera, FPGA implementation or source edit occurred. SSOT stayed revision 9 with no staleness. No firmware repair, I2C reliability qualification, camera readiness or real frame was achieved. The single next action is specified in `NEXT_ONE_ACTION.md`: separately govern one bounded SCL/SDA observation of the Bank5 select data ACK before considering a communication correction.
