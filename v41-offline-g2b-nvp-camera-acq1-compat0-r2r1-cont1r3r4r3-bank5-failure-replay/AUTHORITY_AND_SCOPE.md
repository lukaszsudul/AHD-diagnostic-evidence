# Authority and offline boundary

Owner directive: `CONT1R3R4R3-BANK5-OFFLINE`. This investigation used no DUT connection, JTAG, MMIO device, driver, reboot, programming, NVP write, scan or camera action. Simulation MMIO and pin transitions were local software signals only. Current DUT quiescence and locks are `NOT_APPLICABLE_NO_DUT_ACCESS`, not fresh measurements. The historical endpoint `10.132.1.111:22` was not contacted.

The historical hardware evidence is the 37-file manifest-verified directory at evidence commit `b1b12524d41435aad49d7ce7662af26981682143`. Its original campaign result remains `FAIL`; this directory is additive analysis. The private archive was copied into the fresh run root, checked at 108145 bytes and SHA-256 `96080348A26E00ECF8B4DBA58F6E691EBAD48F2D8B0C3DDC2EC474A316E6A0CB`, safely extracted (34 directory entries; 32 regular files; no absolute/traversal/link entries), and its 26 raw record sizes and hashes all matched the frozen manifest. No failed-attempt raw snapshot was present.

Frozen source authority is `FPGA_AHD` commit `09cd7cbb426027acaefd0cf3989579b80a451f3a`, tree `c6be008ddc387c1f43eefa35d4fed0e2ccb968db`. A task-private `git archive` supplied unchanged scanner, manifest, master, wrapper, top, and host-schema files. Compiled scanner/master/manifest Git blob IDs match the exact commit; the FPGA worktree was not edited or checked out. The bitstream (2192144 bytes, SHA-256 `CD80C84E17467BCB03DEE58DC7FF64D50FD2CB6E5C409E21F871C3E05052BA09`) and routed DCP SHA-256 `C09145B1397E2A8DC972E435E755917C9104A5FDA2E5EF297FE4628EF9672CC2` are provenance only and were not opened.

The six governed `project-current-state` entry documents were read in prescribed order. `PROJECT_STATE_REV_AT_START=9`. SSOT is read-only in this task. The starting snapshot's revision must be checked again before publication; no endpoint or META update is inferred.

Model boundaries: Level A uses a task-owned registered master-response mock with 20-cycle latency to service the unchanged scanner's telemetry writer. Level B uses the unchanged fixed master and scanner at nominal 62.5 MHz with a task-owned digital open-drain slave on resolved SCL/SDA lines. Level B omits PCIe, board analog behavior and physical NVP semantics; source audit verifies the unchanged wrapper forwards master cause/timeout/success to SCAN1 without remapping. Neither level is a physical electrical measurement.
