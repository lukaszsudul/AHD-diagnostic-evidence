# CONT1R3R4 — bounded DUT survey and admission result

Engineering gate: **BLOCKED**. The read-only survey reached the expected DUT at `10.132.1.111:22` with the pinned SSH host key and matched hostname `VCDE-DUT-1` and machine-id `0e90f50d9465492b80258da5658446f8`. The observed boot ID was `32d7b853-06fe-4004-9c4b-9b4abafbe896`. This differs from the last measured CONT1R2 boot recorded in the qualified driver gate (`0c8657ab-9167-48db-9e97-92081c427c99`), but establishes neither the time nor cause of a reboot; the Owner's earlier report remains `POSSIBLE_NOT_CONFIRMED`.

The first unmet admission prerequisite is **FOREIGN_XDMA0_NODE_OWNERSHIP**. The expected target function `0000:01:00.0` is `10ee:7011`, subsystem `10ee:0007`, Gen2 x1, but is unbound. A different PCI function, `0000:0b:00.0`, is bound to the kernel's `xdma` module (`platform:xdma`, `/lib/modules/7.0.0-29-generic/kernel/drivers/dma/xilinx/xdma.ko.zst`). Both `/dev/xdma0_user` and `/dev/xdma0_c2h_0` resolve through sysfs to `0000:0b:00.0`, not the target. The qualified `xdma_ahd_pcie` module was not loaded. No device-holder FD was found for these two nodes during the initial survey, but that does not transfer ownership or authorize unloading the other driver. The `UPSTREAM.lock` file in the historical `g2b_hw0_drv1` artifact directory was observed; it was not treated as a current task-owned hardware lock or removed.

The frozen firmware was present with size **2,192,144 bytes** and SHA-256 `CD80C84E17467BCB03DEE58DC7FF64D50FD2CB6E5C409E21F871C3E05052BA09`. The frozen source commit/tree, signed DCP and 33-file inherited host payload were verified locally. The inherited isolated bundle verifier passed with zero missing resources or module-origin violations and selected the sole current endpoint. The task-specific read-only SSH adapter was syntax-checked and rejected a retired-endpoint literal. This is **not** a DUT bundle-deployment result.

Three bounded, successful read-only SSH survey calls were made. No fresh controller or DUT lock was acquired; no payload was deployed to the DUT; no JTAG, FPGA register, NVP register or XDMA device was opened. No driver was loaded/unloaded, no SRAM programming or reboot occurred, and no control or campaign scan was started. Thus NACK cause, current FPGA image, NVP state, scanner idle and a camera frame remain **unmeasured**. The 1,000-scan and separate 10,000-scan qualification gates remain unrun. No camera, slice, MODE1, EQ, capture or DMA campaign took place.

The task caused no hardware state change. The observed foreign `xdma` binding and its nodes were deliberately left untouched; task cleanup has no owned hardware resource to release. The next practical step is for the Owner to identify the operator/purpose of `0000:0b:00.0` and arrange a governed, non-disruptive resolution of its `xdma0` node ownership before a fresh admission attempt. Do not infer that merely deleting nodes or force-unloading `xdma` is safe. Only after the qualified driver can bind the intended target with proven node-to-BDF mapping can this exact-image characterization proceed.

This report is committed before its own publication; the evidence commit and independent remote read-back are reported separately after publication, not self-certified here. No SSOT, META, PRODUCT, FPGA source, firmware or prior evidence was changed.
