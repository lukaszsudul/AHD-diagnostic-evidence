# Owner authorization

The Owner authorized a direct hardware continuation at `192.168.1.57:22`, classified as `NETWORK_ADDRESS_CHANGE_ONLY`. The exact R2R1 source, DCP, bitstream, and runtime bundle were to be reused without rebuild or source modification. One volatile-SRAM programming, one product-equivalent autoinit, one graceful warm reboot, one exact driver load, read-only runtime qualification, bounded SCAN1/MMIO tests, and—only after the interactive camera gate—the compiled CH1 Bank-5 slice campaign were authorized.

The run stopped before the interactive camera gate. No functional NVP action ID was issued.
