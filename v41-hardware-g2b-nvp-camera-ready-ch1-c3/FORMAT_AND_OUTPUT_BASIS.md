# Format and NVP output basis

The observed NVP chip ID/revision is Bank0/F4=0x90 and Bank0/F5=0x01. In all 20 complete SCAN1 snapshots Bank0/A8 PRE=POST=0x0F, so CH1 NOVID bit0=1. The last three consecutive scans, after exact rollback, are serial 18–20, generations 275–277. They document stable no-video in the restored baseline. Bank5/F2 changes across these scans (0x7C, 0x45, 0x47); the raw format tuple is not stable and is not a recognized format. During slice tests Bank5/F0 reached 0x34, but A8 still indicated no video. It cannot establish AHD, CVI, resolution or frame rate.

The unchanged C3 `format_decision.py` returned `SIGNAL_UNSTABLE` with `NOVID_OR_RAW_TUPLE_NOT_STABLE` because the raw tuple varies. This read-only decoder output remains in `raw/camera-readiness/state.json`. The separate CH1-specific conclusion uses the documented A8 bit0 and is `NO_VIDEO_STABLE`, leading to `NO_VIDEO_AFTER_AVAILABLE_CH1_ACTIONS`. These conclusions address different predicates.

Analog standard=UNRESOLVED; width/height=UNKNOWN; scan mode=UNKNOWN; fps=UNKNOWN. The target AHD1080p25 in the compiled autoinit profile (`range_sel=10`, `channel_sel=00`, `output_sel=A`) is CONFIGURATION_READBACK / source-context evidence only. The static C3 initialization maps CH1 to VDO1 and the existing FPGA input. The SCAN1 output projection on the last scan includes Bank1/84..87=00/01/02/03, Bank1/8C..8F=40/41/42/43, and Bank1/97=0x0F, 98=0x00. This is a configuration observation; no independent fresh live-source route selection was proven.

Existing no-DMA digital observers recorded three 0.5 s windows with VCLK edge deltas 74,276,605 / 74,268,872 / 74,265,964 and active SAV deltas 14,068 / 14,067 / 14,066. `source_status=0xC4`, stream=0, record commits=0. The digital activity can arise from NVP background/test output while CH1 NOVID=1. It does not prove the live camera, complete frame geometry, analog format, or 25 fps. No independent frame-period measurement was available under this scope. Live source selection=NOT_CONFIRMED; output readiness for a live CH1 camera=NOT_CONFIRMED.

First missing condition: a real stable CH1 input with NOVID=0. The source/physical path and codec input require a separately authorized diagnosis. The C3 ACQ campaign is closed after rollback, so the slice commands cannot simply be repeated in this runtime. After any corrective external change, a fresh short SCAN1 and runtime check is required before deciding on capture; no DMA or frame action is authorized here.
